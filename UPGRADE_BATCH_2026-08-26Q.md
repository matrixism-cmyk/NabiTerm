# 업그레이드 배치 Q — 2026-08-26

v0.1.476 이후.

## 이번엔 이름을 지어 찾지 않았다

지난 두 배치에서 같은 실수를 했다 — `connect_group`, `tunnel_status` 같은 **없는 이름**으로
grep하고 "빈틈"이라고 판정했다가, 실제로는 `sessions.connectall`·`fwd_n`으로 이미 있었다.

그래서 이번에는 순서를 뒤집었다: **분야별 파일 목록을 먼저 훑고**, 거기서 안 보이는 것만
후보로 올렸다. 그 방식이 곧바로 성과를 냈다 — 아래 S1은 파일을 읽다가 발견한 것이다.

## 확인 결과

| 후보 | 판정 |
|---|---|
| 호스트키 **변경** 경고 | `handler.rs`가 `Err(_) => Ok(false)` — **뭉개고 있다** |
| 로컬 브라우저 zip 묶기/풀기 | 브라우저 i18n 키 27개 어디에도 없다 |
| 로컬 브라우저 최근 경로 | 없다(SFTP에만 있다 — v0.1.476에서 만든 것) |
| 블록 하나의 출력만 꺼내기 | `last_command_output`은 **마지막 것만** |
| 세션 로그 자동 시작 | `toggle_session_log` 수동뿐, 설정 없음 |
| 같은 낱말 모두 고르기 | 다중 커서는 **세로 추가**(`add_cursor_vertical`)뿐 |
| 전송 히스토리 | **이미 있다**(`sftphistory`) — 건너뜀 |
| 여러 파일에서 바꾸기 | **이미 있다**(`replaceui`) — 건너뜀 |
| 동기 탐색(로컬↔원격) | **이미 있다**(`sftpact`) — 건너뜀 |
| 자동 저장 | **이미 있다**(`autosave_at`) — 건너뜀 |
| 터미널 찾기 정규식·개수 | **이미 있다**(`find.rs`) — 건너뜀 |

## 계획 (12 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| S1 | SSH | **호스트키가 바뀌면 그렇다고 말한다** | 지금은 이유 없이 접속 실패한다 | **높음(보안)** |
| S2 | SSH | 바뀐 키를 **확인 후 교체**하는 길 | 서버를 새로 세우면 정상적으로 바뀐다 | 높음 |
| T1 | 셸 | 블록 목록에서 **그 블록 출력만** 꺼내기 | 마지막 것만 복사할 수 있다 | 낮음 |
| T2 | 셸 | **세션 로그 자동 시작** 설정 | 켜는 것을 잊으면 기록이 없다 | 낮음 |
| B1 | 브라우저 | 고른 파일 **zip으로 묶기** | 올리기 전에 묶는 일이 잦다 | 중 |
| B2 | 브라우저 | **zip 풀기**(여기에 / 새 폴더에) | 받은 것을 풀려면 밖으로 나가야 한다 | 중 |
| B3 | 브라우저 | 로컬도 **최근 경로** 기억 | SFTP에만 있다 | 낮음 |
| B4 | 공통 | 최근 경로 규칙을 **한 곳으로**(SSOT) | 두 벌이 되면 어긋난다 | 낮음 |
| E1 | 에디터 | **같은 낱말 모두 고르기**(다중 커서) | 다중 커서가 세로 추가뿐이다 | 중 |
| E2 | 에디터 | **다음 일치에 커서 추가**(점증) | 하나씩 늘려 가는 쪽이 실수를 줄인다 | 중 |
| M1 | 메뉴/IA | 브라우저 메뉴 그룹화 + 새 기능 노출 | 매 배치 IA phase | 낮음 |
| M2 | 메뉴/IA | 설정 색인·i18n 드리프트 배선 | 검사기가 잡는 곳 | 낮음 |

## 가장 조심할 것 — 호스트키 변경(S1·S2)

**중간자 공격과 서버 재설치가 똑같이 생겼다.** 우리는 둘을 구별할 방법이 없다. 그래서:

* 기본은 **거부**다. 지금 동작(연결 실패)이 안전한 쪽이라는 것은 맞다 — 틀린 것은 **아무
  말도 안 한다**는 점이다.
* 바뀌었다는 사실, **옛 지문과 새 지문**, 어느 줄인지를 보여 준다.
* 교체는 **한 번 더 명시적으로** 누를 때만. 실수로 눌리는 자리에 두지 않는다.
* 자동 학습(TOFU)은 **미지 호스트에만** 남긴다. 바뀐 키를 자동 학습하면 TOFU의 뜻이 사라진다.

## 결과 (v0.1.477 예정)

### S1·S2 — 호스트키가 바뀌면 그렇다고 말한다 (이번 배치의 핵심)

`handler.rs`가 이렇게 되어 있었다:

```rust
    Err(_) => Ok(false),   // KeyChanged도 여기로 빨려 들어갔다
```

거부한다는 판단은 옳다. 틀린 것은 **아무 말도 하지 않는다**는 점이었다 — 사용자에게는
이유 없이 안 붙는 서버로 보이고, 왜인지 알 길도 고칠 길도 없었다. OpenSSH는 이 상황에서
화면 가득 경고를 찍는다. 우리는 침묵했다.

이제 갈래를 나눠 사실을 올려 보낸다: **어느 줄**이 걸렸는지, 그 줄의 **옛 지문**이
무엇인지. 확인창은 제목·색·문구가 바뀌고 옛/새 지문을 나란히 보여 준다.

판단은 하지 않는다. **중간자 공격과 서버 재설치는 겉으로 똑같이 생겼고 우리는 구별할 수
없다.** 그래서:

- 기본은 거부. 받아들이려면 "관리자와 대조했다"는 칸을 켜야 단추가 살아난다.
- 자동 학습(TOFU)은 **미지 호스트에만** 남겼다. 바뀐 키를 자동으로 배우면 TOFU의 뜻이 사라진다.
- 받아들이면 **옛 줄을 먼저 지운다**. 안 지우면 다음 접속에도 같은 경고가 뜬다.
- 지울 때는 **그 줄만**. 한 호스트가 여러 알고리즘 키를 가질 수 있어, 하나 때문에
  나머지까지 지우면 멀쩡한 신뢰를 잃는다.

**시험이 결함을 잡았다.** 줄 번호를 `saturating_sub(1)`로 다루다 0번을 물으면 1번 줄의
지문이 나왔다. 대조하는 화면에서 **엉뚱한 옛 지문**보다 나쁜 결함은 없다.

### B1·B2 — zip 묶기·풀기

**푸는 쪽이 위험하다.** zip 안의 경로는 만든 쪽이 정한다. `../../Windows/...`가 들어
있으면 그대로 이어 붙이는 순간 푸는 폴더 밖에 파일을 쓴다(zip slip). 경로 판단을 순수
함수로 떼어 시험 넷을 붙였고, 걸린 항목은 건너뛰되 **세어서 화면에 말한다.**

푼 것은 현재 폴더에 쏟지 않고 `이름/` 폴더에 넣는다 — 수십 개가 섞이면 무엇이 원래
있던 것인지 알 수 없다.

### B3·B4 — 최근 경로를 한 곳으로

지난 배치에 원격(SFTP)에만 만든 것을 로컬에도 넣으면서 **규칙을 베끼지 않았다.**
`sftprecent` → `recentpaths`로 이름을 넓히고 두 곳이 같은 함수를 부른다. 베꼈다면
언젠가 한쪽만 고쳐져 "원격은 스무 개인데 로컬은 열 개"가 됐을 것이다.

### T1 — 블록 하나의 출력만

`last_command_output`은 **마지막 것만** 준다. 목록에서 고른 블록을 꺼내려면 자리를
받아야 한다. 곁가지로 `EditorDoc::make(...)` 여덟 인자를 세 곳이 되풀이하던 것을
`open_text_as_doc` 하나로 모았다 — `dirty` 켜는 것도 자리마다 따로였다.

### T2 — 세션 로그 자동 시작

로그가 필요해지는 순간은 대개 **이미 지나간 뒤**다. 그런데 지금까지는 손으로 켜야 했다.
**기본은 꺼짐** — 터미널에는 비밀번호도 남의 데이터도 지나간다. 묻지 않고 디스크에
남길 일이 아니다.

### E1 — 조용한 상한을 말하게

에디터는 포화였다(아래 참고). 대신 같은 결함 클래스를 하나 찾았다: `select_all_matches`가
커서 **1만 개에서 자르고 아무 말도 하지 않았다.** 전부 잡힌 줄 알고 편집하면 나머지는
안 바뀐 채 남는다. 이제 상태바 개수 옆에 `+`가 붙고 툴팁이 까닭을 말한다.

## 계획이 또 틀렸다 — 이번엔 파일 목록도 부족했다

E1·E2로 계획한 "같은 낱말 모두 고르기"는 **이미 완전히 있었다**(`editbufmatch.rs` +
전용 시험 파일 + 메뉴·키 배선까지). 심지어 내가 만든 것보다 낫다 — 수백 MB 문서에서
`rope.to_string()` 사본을 만들지 않으려고 청크를 훑는다.

지난 배치의 교훈("이름을 지어 찾지 말고 파일 목록을 훑어라")을 따랐고, 목록에
`editbufmatch.rs`·`editbufmatchtest.rs`가 **분명히 있었는데 눈으로 지나쳤다.** 목록을
보는 것만으로는 부족하다 — **관련돼 보이는 이름은 열어 봐야 한다.**

만든 것은 지웠다(`selmatch.rs`, `editbufmatchsel.rs`). 다만 그 과정에서 얻은 것이
있다: 상한이 조용하다는 사실은 그 파일을 **읽었기 때문에** 보였다.

## 검증

```text
clippy 0 · lines 신규 초과 0(배치 전과 같은 36) · prerelease 0 · unsafe 근거없음 0
```
