# 업그레이드 배치 P — 2026-08-26

v0.1.475 이후. 계획 전 후보마다 동의어 대여섯 개로 확인했다(규율).

## 확인 결과

| 후보 | 판정 |
|---|---|
| 명령 소요 시간 | `elapsed`·`took_ms`·`명령 시간` — 블록에 **시간이 없다** |
| 분할 창 동기 스크롤 | `sync_scroll`·`scroll_lock` **없음** |
| 그룹 한 번에 접속 | `connect_group`·`그룹 접속` **없음** |
| 열린 터널 한눈에 | `ForwardForm.active`는 있으나 **대화상자 안에서만** 보인다 |
| 원격 파일 비교 | `compare_remote` **없음**(로컬 비교 `difflines`는 있다) |
| 최근 원격 경로 | 북마크(`sftp_bookmarks`)는 있으나 **자동 기록은 없다** |
| 괄호 자동 닫기 | `auto_pair` **없음**(선택 감싸기 `ctx.surround`는 있다) |
| 버퍼 단어 자동완성 | LSP 완성(`lspcomp`)만 있고 **평문 파일엔 없다** |
| 최근 편집 위치로 이동 | `jump_last_edit` **없음** |
| 일괄 이름 변경(SFTP) | **이미 있다**(`sftpentries::batch_rename`) — 건너뜀 |
| 느린 명령 알림 | **이미 있다**(`slowcmd`) — 건너뜀 |
| 자동 들여쓰기 | **이미 있다**(`editbufedit`) — 건너뜀 |

## 계획 (12 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| T1 | 셸 | **명령마다 걸린 시간** 기록·표시 | 블록에 결과는 있는데 시간이 없다 | 낮음 |
| T2 | 셸 | **분할 창 동기 스크롤** | 로그 둘을 나란히 견주는 일이 잦다 | 중 |
| S1 | SSH | **그룹을 한 번에 접속** | 아침마다 같은 서버 넷을 하나씩 연다 | 중 |
| S2 | SSH | **열린 터널을 어디서나** 보고 끄기 | 지금은 그 대화상자를 다시 찾아야 한다 | 낮음 |
| F1 | SFTP | **원격 파일 둘을 비교** | 로컬 비교는 되는데 원격은 받아야 한다 | 중 |
| F2 | SFTP | **다녀온 경로 기억**(최근 목록) | 북마크는 손으로 찍어야 한다 | 낮음 |
| M1 | 메뉴/IA | 이번 기능 노출 정리 + 중복 점검 | 매 배치 IA phase | 낮음 |
| M2 | 메뉴/IA | 설정 화면에 새 항목 배선(드리프트 방지) | 검사기가 잡는 곳 | 낮음 |
| E1 | 에디터 | **괄호·따옴표 자동 닫기** | 편집기의 기본기 | 중 |
| E2 | 에디터 | **버퍼 단어 자동완성**(LSP 없는 파일) | 설정·로그 파일엔 LSP가 없다 | 중 |
| E3 | 에디터 | **마지막 고친 자리로 이동** | 찾다가 자리를 잃으면 되돌아갈 길이 없다 | 낮음 |
| E4 | 에디터 | 자동 닫기와 감싸기를 한 규칙으로(SSOT) | 두 곳이 짝을 따로 알면 어긋난다 | 낮음 |

## 계획이 틀린 곳 — SSH는 이미 포화였다

계획에 적은 SSH 항목 둘이 **이미 있었다.** 확인할 때 지어낸 이름으로만 찾은 탓이다:

| 계획한 것 | 실제 | 내가 찾은 이름 | 진짜 이름 |
|---|---|---|---|
| 그룹 한 번에 접속 | 있음 | `connect_group` | `sessions.connectall` / `ConnectFolder` |
| 열린 터널 한눈에 | 있음 | `tunnel_status` | `statusbar.rs`의 `fwd_n` 칩 |
| 자동 재접속 | 있음 | `reconnect_attempts` | `begin_auto_reconnect` |

지난 배치에서 세 번 겪은 일이 또 나왔다. 이름을 지어 찾으면 **없다는 답만 얻는다.**
그래서 이번엔 방향을 바꿔 `crates/nabi-ssh/src`와 `crates/nabi-app/src`의 **파일 목록을
눈으로 훑었고**, 그때 비로소 진짜 빈틈 둘이 보였다(접속 시간 제한, `Include`).

SSH가 포화라는 사실 자체가 결과다. 스킬이 "분야가 포화면 다른 분야로 보충하라"고 적어
둔 대로 무게를 SFTP·에디터로 옮겼다.

## 결과 (v0.1.476 예정)

### T1 — 명령마다 걸린 시간

블록에 결과(종료 코드)는 있는데 **시간이 없었다.** 프롬프트 표식에 시각을 얹고, 끝날 때
재서 목록에 보인다. 못 잰 것은 **비워 둔다** — 0으로 채우면 "눈 깜짝할 새 끝났다"와
"재지 못했다"가 같아 보인다.

글자 꼴은 순수 함수(`human_ms`)로 떼어 시험 여섯을 붙였다. 그중 둘이 경계를 지킨다:
1초 미만이 `0s`가 되지 않을 것, 그리고 **하루가 넘어도 자릿수가 끝없이 늘지 않을 것**
(999일에서 끊고 `999d+`라고 말한다 — 잘라 놓고 아닌 척하지 않는다).

### T2 — 분할 창 동기 스크롤

곁가지가 본줄기였다. 동시 입력이 쓰던 **"그룹을 안 정했으면 이 창 전부"** 규칙이 함수
안에 묻혀 있었는데, 동기 스크롤이 같은 뜻을 필요로 했다. 두 벌이 되면 "입력은 넷에
갔는데 스크롤은 셋만 움직이는" 일이 생긴다. 규칙을 `panegroup`으로 꺼내 시험 다섯을
붙이고, **동시 입력도 그 함수를 쓰도록 고쳤다.**

교착도 조심했다 — 한 pane의 자물쇠를 쥔 채 다른 pane의 자물쇠를 잡지 않는다.

### S1 — 접속 시간 제한을 설정으로

15초가 코드에 박혀 있었다. 폐쇄망·위성 회선에서는 붙을 수 있는 서버를 **못 붙는다고**
답하게 하는 값이다. 다만 그냥 열어 주면 안 되는 까닭이 있다: 이 시간에는 **호스트키
확인창을 사람이 읽는 시간**이 들어간다. 짧게 정해 두면 지문을 확인하고 신뢰를 누른
순간 이미 끊겨 있다.

그래서 규칙을 `conntimeout` 한 곳에 두었다 — 확인창이 뜨는 접속에는 늘 읽을 시간을
주고, 터무니없는 값은 양쪽에서 막는다. 설정이 ssh 층에 닿는 길도 keepalive와 **같은
길**을 쓴다(길이 둘이면 어느 쪽이 이기는지 아무도 모른다).

### S2 — `~/.ssh/config`의 `Include`

요즘 설정은 `Include config.d/*`로 쪼개져 있다. 가져오기가 그 줄을 그냥 지나쳐서,
**여러 파일로 나눈 사람은 세션이 거의 안 들어왔다.**

펼치는 층을 따로 두어 파서를 파일 시스템에서 떼어 놨다. 조심한 것은 **고리**다 —
서로를 포함하는 두 파일에서 멈추지 않으면 프로그램이 선다. 본 파일은 다시 읽지 않고,
다섯 겹에서 끊는다. 시험 일곱 중 하나가 그 고리를 건다.

### F1 — 원격 파일 둘 비교

**새 비교를 만들지 않았다.** 받아 온 뒤에는 로컬 비교와 같은 입구(`compare_paths`)로
들어간다 — 이진이냐 글이냐를 가르는 판단까지 한 곳이다. 그러려고 기존 함수를 경로
기반으로 쪼갰다.

임시 이름에 a/b 표시를 넣은 것은 **다른 폴더의 같은 이름**을 견주는 일이 흔하기
때문이다. 안 넣으면 하나가 다른 하나를 덮어 자기 자신과 비교하게 된다.

### F2 — 다녀온 경로 기억

북마크는 손으로 찍어야 한다. 정작 필요한 것은 "아까 그 폴더"이고, 그건 찍어 둘 만큼
중요하지 않아서 안 찍어 둔 곳이다. **도착했을 때** 기억한다(보낼 때 기억하면 없는
폴더도 목록에 남는다). 호스트를 함께 적는다 — `/var/log`는 서버마다 다른 곳이다.

새 메뉴를 만들지 않고 별표(북마크) 메뉴 아래에 붙였다. 같은 주제를 두 군데로 나누면
찾는 사람이 두 곳을 다 열어 봐야 한다.

### E1·E4 — 괄호 자동 닫기(감싸기와 한 표로)

감싸기 메뉴가 이미 짝 목록을 들고 있었다. 자동 닫기가 자기 목록을 또 만들면 언젠가
한쪽에만 글자가 늘어 **감싸기로는 되는데 자동으로는 안 닫히는** 짝이 생긴다. 표를
`pairs` 한 곳에 두고, 두 표가 어긋나면 실패하는 시험을 붙였다.

성패를 가르는 것은 **언제 닫지 않는가**다. `don't`를 치는데 `don''t`가 되면 그 기능은
꺼진다. 뒤에 글자가 붙어 있으면 닫지 않는다. 부등호(`<`)는 자동 목록에서 뺐다 —
부등호로 쓰는 일이 훨씬 많다.

고른 글은 **지워지지 않고 둘러진다.** 이걸 틀리면 사용자의 글이 사라진다.

### E2 — 버퍼 낱말 완성

LSP 완성은 서버가 붙는 파일에서만 된다. 설정·로그·스크립트·메모에는 서버가 없는데,
**긴 낱말은 거기에도 있다.** 문서에 이미 쓰인 낱말만 권한다 — 무엇이 옳은지 판단하지
않으니 오히려 예측 가능하다.

**계획과 달라진 것 — 목록 팝업을 만들지 않았다.** 팝업을 커서 옆에 띄우려면 이 편집기가
갖고 있지 않은 **커서 화면 좌표**가 필요하고, 그것을 만들려면 그리는 코드를 손봐야 한다.
그 자리는 과거에 두 번 사고가 난 곳이다(갤리 배칭 밑줄 어긋남 v0.1.33 철회, `painter.rs`
인덱스 초과로 렌더 스레드 즉사 v0.1.41).

대신 **되풀이해 누르면 다음 후보로 도는** 방식을 썼다. Vim의 `Ctrl-N`, Notepad++의 낱말
완성이 오래 써 온 조작이라 목록 없이도 손에 익는다. 한 바퀴 돌면 **치던 글자로
되돌아온다** — 후보에 갇히지 않고 빠져나갈 길이 있어야 한다.

### E3 — 마지막 고친 자리로

찾다가 자리를 잃으면 돌아갈 길이 없었다. 되돌리기로 돌아갈 수는 있지만 그건 **고친
것을 무르는** 일이라 뜻이 다르다.

한 자리만 기억하면 이미 거기 서 있을 때 눌러도 아무 일이 없다. 그래서 최근 여덟 자리를
두고 돌아간다. 한 줄을 고치는 동안 글자마다 자리를 남기면 목록이 그 줄로 가득 차므로,
붙어 있는 자리는 최신 것으로 덮는다.

### M1·M2 — 노출과 드리프트

새 기능을 메뉴·팔레트·설정에 모두 배선했다. 설정 검색 색인을 빠뜨리면 검사기가 잡는다
(지난 배치에 실제로 잡혔다). 이번에는 넣고 시작했다.
