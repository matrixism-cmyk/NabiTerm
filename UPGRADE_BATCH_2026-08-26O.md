# 업그레이드 배치 O — 2026-08-26

v0.1.474 이후. 계획 전 동의어 대여섯 개씩으로 확인했다(규율).

## 확인 결과

| 후보 | 판정 |
|---|---|
| 원격 명령 실행 | `SftpExec`·`RemoteExec`·`exec_command`·`ssh_exec`·`channel_exec` **전부 없음** |
| 실패한 명령으로만 이동 | `jump_failed`·`prev_failed`·`failed_prompt` **없음** |
| 명령 블록 목록 창 | `block_list`·`command_list` **없음** |
| 세션 표식으로 거르기 | 사이드바 거르기는 **글자만** 본다(`session_matches`) |
| 편집기 탭 고정 | `pin_tab`·`tab_pinned` **없음** |
| 폴더를 서버 안에서 복사 | v0.1.474는 **파일만** 된다 |
| 명령 출력 접기 | 없지만 **이번엔 안 한다**(아래 참고) |

## 접기는 이번에 하지 않는다

터미널 출력을 접으려면 렌더러에 "소스 줄 ↔ 화면 줄" 사이의 층을 넣어야 한다. 그 자리는
과거에 두 번 사고가 난 곳이다 — 갤리 배칭으로 밑줄이 어긋났고(v0.1.33 철회), `painter.rs`의
인덱스 초과로 **렌더 스레드가 즉사**했다(v0.1.41). 성능에 민감하고 매 프레임 도는 코드다.

접기 대신 **같은 목적을 다른 길로** 푼다: 긴 출력에서 원하는 것을 찾는 일은 이미 있는
걸러보기(`findall`)가 하고, 이번 배치의 **블록 목록**이 "어떤 명령이 있었나"를 답한다.
접기는 렌더러를 손볼 여유가 있는 배치에서 따로 다룬다.

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| S1 | SSH | **고른 파일에 원격 명령** — 코어(명령 만들기·인용) | 터미널로 건너가 경로를 다시 친다 | 중 |
| S2 | SSH | 실행 전 **무엇이 실행될지 보여 주기** | 서버에서 명령을 도는 일이다. 위험할수록 먼저 본다 | 중 |
| S3 | SSH | 실행·결과 창(읽기 전용·취소 가능) | 실행만 하고 결과를 못 보면 반쪽 | 중 |
| T1 | 셸 | **실패한 명령으로만 이동** | 종료 코드는 이미 블록에 있다. 훑는 길만 없다 | 낮음 |
| T2 | 셸 | **명령 블록 목록** — 명령·결과를 한눈에, 눌러서 이동 | 긴 로그에서 "그 명령"을 되찾는 길 | 중 |
| F1 | SFTP | **폴더도 서버 안에서 복사**(재귀) | v0.1.474는 파일만. 폴더는 아직 받았다 올려야 한다 | 중 |
| M1 | 메뉴/IA | 이번 기능의 노출 정리 | 매 배치 IA phase | 낮음 |
| M2 | 메뉴/IA | **세션을 표식으로 거르기**(운영만 보기) | 지금 거르기는 글자만 본다 | 낮음 |
| E1 | 에디터 | **탭 고정** — 실수로 닫히지 않게 | 문서를 여럿 열면 닫기 실수가 잦다 | 낮음 |
| E2 | 에디터 | 고정 탭은 "다른 탭 닫기"에서 살아남는다 | 고정의 뜻이 거기 있다 | 낮음 |
| E3 | 에디터 | 고정 상태를 작업 공간 복원에 반영 | 껐다 켜면 풀리면 고정이 아니다 | 중 |

## 가장 조심할 것 — 원격 명령(S1~S3)

**서버에서 명령을 실행하는 기능이다.** 좁게 만든다.

* **정해 둔 명령만.** 사용자가 자유롭게 타이핑하는 칸이 아니라, 목록에서 고른다
  (압축·해제·해시·용량). 자유 입력은 터미널이 이미 있고, 그게 더 나은 도구다.
* **파일 이름은 인용한다.** 이름에 공백·따옴표·`;`·`$`가 있으면 그대로 명령이 된다.
  인용 규칙은 **순수 함수 + 시험**으로 만들고, 악의적 이름을 시험에 넣는다.
* **실행 전에 전문을 보여 준다.** 붙여넣기 확인창과 같은 생각이다.
* 결과는 **읽기 전용**, 실행 중 **취소 가능**.

---

## 결과 (v0.1.475 예정)

### S1~S3 — 고른 파일에 원격 명령

`remotecmd.rs`(명령 짓기·인용) + `remotecmdui.rs`(확인창·결과 창) + exec 통로.

**인용이 이 기능의 전부다.** 파일 이름은 사용자가 짓지 않는다 — 서버에 있는 것을 그대로
받는다. 그 안에 `;`나 `$(...)`가 있으면 인용하지 않는 순간 **이름이 명령이 된다.**
시험 11건 중 넷이 그것만 본다(악의적 이름·명령 치환·개행·작은따옴표).

안전을 위해 좁힌 것:

- **자유 입력 칸을 만들지 않았다.** 정해 둔 일곱 가지만 고른다. 아무 명령이나 치는 일은
  터미널이 이미 더 잘한다.
- **실행 전에 전문을 보여 준다** — 인용이 어떻게 됐는지까지 그 화면에서 보인다.
- 파일을 **바꾸는** 명령은 표시를 하나 더 단다. 보기만 하는 것과 같은 무게로 물으면
  확인창이 습관이 되고, 그러면 정작 위험한 것도 넘기게 된다.
- 출력은 **상한에서 끊고 끊겼다고 말한다**(서버가 기가바이트를 뱉을 수 있다).
- 표준오류를 **함께** 모은다. 따로 두면 실패했을 때 화면이 비어 보인다.

**계획과 달라진 것 — 취소는 못 만들었다.** 계획에 "실행 중 취소 가능"이라고 적었는데
구현하지 않았다. 채널을 닫아도 서버 쪽 프로세스는 계속 도는 경우가 많아, "취소했다"고
말하고 실제로는 안 멈추는 단추가 된다. 그런 단추는 없느니만 못하다. 대신 **출력 상한**으로
우리 쪽이 끝없이 매달리는 일은 막았다. 진짜 취소는 서버에 신호를 보내는 문제라 따로 다룬다.

### ⚠️ 실서버 시험이 진짜 결함을 잡았다

```text
  assertion failed: 성공한 명령의 종료 코드가 0이 아니다
```

출력은 오는데 **종료 코드만 늘 비어 있었다.** 원인: 채널 루프가 `Eof`에서 끊고 있었는데,
서버는 대개 `Eof`를 먼저 보내고 **그 뒤에** `ExitStatus`를 보낸다.

인프로세스 서버는 exec 자체를 구현하지 않아 **이 경로는 실서버에서만 볼 수 있다.**
스킬 §4가 "실제로 두 번 다 실서버에서만 결함이 나왔다"고 적어 둔 이유를 다시 확인했다.

### 상한 시험은 정직하게 건너뛴다

많이 뱉게 하는 방법이 셸마다 다르다(`yes | head`는 POSIX 전용). 이 머신의 서버는
Windows OpenSSH라 기본 셸이 `cmd.exe`다. **조용히 통과시키면 검증한 척**이 되므로,
`uname`으로 먼저 보고 아니면 **건너뛴다고 말하고** 넘어간다.

같은 이유로 명령 목록이 POSIX 전용이라는 사실을 모듈 주석과 메뉴 안내에 적었다. 다만
**우리가 미리 막지는 않는다** — Windows 서버에도 Git for Windows 경유로 깔려 있을 수
있고 우리는 그것을 알 방법이 없다. 되는지는 서버가 답하게 두고 그 답을 가리지 않는다.

### T1 — 실패한 명령으로만 이동

종료 코드는 이미 블록에 있었다. 없던 것은 **거르개**뿐이라, `jump_failed_prompt` 하나와
시험 둘로 끝났다. 아무것도 안 움직였을 때 말해 주는 것을 잊지 않았다 — 눌렀는데 조용하면
고장으로 읽힌다.

### T2 — 명령 블록 목록 (`nabi-vt/blocklist.rs`)

표식마다 그 줄의 글자와 종료 코드가 이미 손에 있었으므로, **모아 주기만** 하면 목록이 된다.
최신이 앞에 온다(되찾고 싶은 것은 거의 항상 방금 것이다).

시험이 잡은 것 하나: 마지막 블록의 출력 줄 수가 **한 줄씩 부풀어** 있었다. 커서가 앉은
빈 줄까지 세고 있었던 것이다. 다른 블록은 다음 프롬프트에서 끊기므로 이 문제가 없어,
눈으로는 영영 못 봤을 차이다.

### M2 — 세션을 표식으로 거르기

**새 거르개를 만들지 않았다.** 이미 있는 칸이 이름·호스트를 보고 있었으므로 거기에 표식
낱말을 더했다. 칸이 둘이 되면 사용자는 어느 쪽에 쳐야 하는지 매번 생각해야 한다.

낱말은 **영어 고정**(prod/staging/dev/note)이다 — `nabi-session`은 UI 언어를 모른다.
대신 외울 필요가 없도록 사이드바에 **칩 셋**을 뒀다: 누르면 칸에 그 낱말이 들어간다.

### F1 — 폴더도 서버 안에서 복사 (`nabi-sftp/copytree.rs`)

**자기 안으로 복사하는 것을 먼저 막았다.** `/a`를 `/a/b`로 복사하면 만들어진 것을 다시
읽으며 디스크가 찰 때까지 내려간다. 판단은 순수 함수(`is_inside`)로 떼어 시험 다섯을
붙였다 — 그중 하나는 `/a`와 `/ab`를 가르는 것이다(글자로만 견주면 틀린다).

링크는 **따라가지 않고 건너뛰되 세어 둔다**. 조용히 빠뜨리면 복사가 덜 된 줄 모른다.

### 곁가지로 나온 결함 클래스 — 오류가 키로 새어 나오고 있었다

아래 층(SFTP·전송)은 UI 언어를 모르므로 i18n **키**를 오류 문자열로 올려보낸다. 원격
명령 창만 그중 하나를 손으로 바꾸고 있었고, 전송 큐는 **키를 그대로 보여 주고** 있었다.
자리마다 늘어놓는 대신 `errkey::human` 한 곳으로 모았다.

그 시험이 진짜 함정을 잡았다: **`tr`은 모르는 키에 `"?"`를 돌려준다.** 그대로 썼다면
화면에 물음표 하나만 떠서 무엇이 잘못됐는지 아무도 모를 뻔했다. 서버가 보낸 진짜
오류문은 **건드리지 않는다** — 우리 말로 덮으면 원인을 잃는다.

### E1~E3 — 탭 고정

계획에는 "편집기 탭"이라 적었는데 **모든 탭**에 뒀다. 자리를 지키고 싶은 것은 문서만이
아니고(운영 서버 셸이 더 그렇다), 드는 품이 같았기 때문이다.

고정하면 **닫기 단추가 사라진다**(못 닫게 하는 것이 아니라 실수로 누르는 일을 막는 것이다 —
우클릭에서 풀면 닫힌다), "다른 탭 닫기"에서 살아남고, 껐다 켜도 남는다. 마지막 하나가
빠지면 고정이 아니라 이번 판 표시일 뿐이다.

"다른 탭 닫기"의 규칙은 순수 함수로 떼어 시험했다 — **되돌릴 수 없는 일**이라 눈으로
확인할 수 있어야 한다.

### M1 — 메뉴 정리

편집 메뉴에 흩어져 있던 명령 블록 항목 셋(마지막 출력 복사·이전/다음 실패)을 새 목록과
함께 **`명령 블록` 한 묶음**으로 넣었다. 항목 넷이 최상위에서 사라지고 하나가 생겼다.

### 검증

```text
clippy 0 · lines 경고 35(신규 0) · prerelease 0 · unsafe 근거없음 0
cargo test --workspace  →  1413 통과 · 실패 0
실서버(OpenSSH)         →  23 통과 (폴더 복사·자기 안 거절 포함)
xtask e2e               →  통과
```
