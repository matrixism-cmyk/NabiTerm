# 배치 2026-08-25j — 11 phase, 다섯 분야

v0.1.469 직후. 지난 배치에서 세운 규칙([[feedback-use-what-is-built]])대로 **"구현됐는데
호출 0회"부터 훑었다.** 크레이트 경계를 넘는 미사용은 clippy가 못 잡으므로 손으로 센다.

```bash
for f in $(grep -rhoP '^\s*pub (?:async )?fn \K\w+' crates/*/src); do
  n=$(grep -rhoP "\b$f\s*\(" --include=*.rs crates | wc -l); [ "$n" -le 1 ] && echo "$f"
done
```

두 건이 나왔다.

* `nabi-ssh::agent::agent_identities` — 에이전트가 든 키 목록. **호출 0회.**
* `nabi-editor::extsession::EditSession` — 외부 편집기 세션 전체. **한 번도 만들어지지 않는다.**

## 그 밖에 찾은 결함

**로그가 지워지지 않는다.** `tracing_appender::rolling::daily`는 날마다 새 파일을 만들지만
**보관 기간이 없다.** 매일 쓰면 1년에 365개가 쌓인다. 사용자 폴더가 조용히 부푼다.

**외부 편집기를 고를 수 없다.** 원격 파일을 외부에서 편집하는 길은 있는데(`edit_remote`)
**OS 기본 앱으로만** 연다(`cmd /C start`). VS Code로 열고 싶어도 방법이 없다.
WinSCP가 오래 갖고 있는 기능이고, `extsession.rs`는 바로 그것을 위해 만들어졌다가 잊혔다.

## phase

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| T1 | 셸 | **오래된 로그 정리**(보관 일수 설정, 시작 시 1회) | 위 | 낮음(삭제라 신중히) |
| T2 | 셸 | 명령 기록 **퍼지 검색**(지금은 부분일치만) — 팔레트 matcher 재사용 | `cargo tst` → `cargo test` | 낮음 |
| S1 | SSH | **에이전트가 든 키 보기**(`agent_identities` 활용) | 접속 전에 될지 안다 | 낮음 |
| S2 | SSH | 에이전트 인증을 골랐는데 **키가 없으면 미리 알린다** | 실패하고 나서 알면 늦다 | 낮음 |
| F1 | 파일관리자 | **외부 편집기 지정**(비우면 OS 기본 — 기존 동작) | 위 | 중 |
| F2 | 파일관리자 | 죽은 `extsession.rs` **정리** | 기능처럼 보이는 죽은 코드는 읽는 사람을 속인다 | 낮음 |
| M1 | 메뉴/IA | **설정 '사용자 규칙' 페이지 정리** — 강조·알림·자동응답·링크규칙이 한 장에 쌓였다 | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | **최근 닫은 문서 다시 열기** | 터미널 탭에는 있는데(closed_sessions) 문서에는 없다 | 낮음 |
| E2 | 에디터 | **들여쓰기 안내선** | 세로 눈금의 짝 — 중첩 깊이를 눈으로 본다 | 중 |
| E3 | 에디터 | 안내선·눈금을 한 설정 묶음으로(둘 다 "보조선") | 드리프트 방지 | 낮음 |
| T3 | 셸 | 로그 정리 결과를 **진단 묶음에 적는다** | 지운 것을 말하지 않으면 사라진 것처럼 보인다 | 낮음 |

## 위험이 가장 큰 것 — T1(파일 삭제)

로그 정리는 **사용자 디스크의 파일을 지운다.** 규칙을 코드로 못 박는다.

* 지우는 것은 **우리가 만든 이름**(`nabi.log.YYYY-MM-DD`)뿐이다. 폴더 안의 다른 것은 안 건드린다.
* **오늘 파일은 절대 안 지운다**(쓰고 있는 중이다).
* 기본 보관 30일. 0이면 **정리하지 않는다**(끄는 길을 반드시 둔다).
* 지운 개수를 진단 묶음에 남긴다.

## 결과 — 11 phase 전부

## 하다가 알게 된 것

**앞 배치에서 넣었다고 여긴 줄이 실제로는 안 들어가 있었다.** 시작 시 링크 규칙을 렌더러에
넣는 `set_rules` 호출이 빠져 있어, **설정 창을 열어야만 규칙이 먹는 상태**였다. 그 편집을
하던 파이썬이 이스케이프 문법 오류로 중단됐는데 뒤이은 출력만 보고 성공으로 여겼다.
→ 편집 뒤에는 **결과를 grep으로 확인**해야 한다. 스크립트가 여러 파일을 고칠 때 특히 그렇다.

**`git checkout -- <파일>`이 이번 배치의 작업도 함께 지웠다.** 분할을 잘못해 되돌리려고
썼는데, 그 파일에는 커밋되지 않은 이번 배치 작업(M1 그룹화·안내선 체크박스)도 있었다.
다시 넣었다. → 되돌리기 전에 **그 파일에 무엇이 얹혀 있는지** 먼저 볼 것.

**함수 경계를 잘못 잡아 파일을 반으로 잘랐다.** 문서 주석까지 거슬러 올라가는 자동 분할이
`editor_settings_body` 중간을 끊었다. 되돌리고, 대신 문장 병합으로 한도를 맞췄다 —
4줄 넘은 것을 고치자고 큰 분할을 할 이유가 없었다.

### 실서버 검증

에이전트 키 조회는 **켜고 끄고 둘 다** 확인했다(스킬 §4 요구).

* 켠 상태: `[on] 키 1개: ["nabi-agent-test (ssh-ed25519)"]`
* 끈 상태: `[off] 키 0개: []` — **터지지 않고** 빈 목록. 사용자에게는 "이대로 붙으면
  실패합니다" 경고로 나타난다. 이 저장소는 예전에 꺼진 에이전트에서 "early eof"가
  사용자에게 그대로 노출되던 결함을 겪었으므로, 이쪽을 반드시 본다.

검증용 키·서비스 상태는 **원상복구**했다(`Stopped/Disabled`, authorized_keys 흔적 0).
