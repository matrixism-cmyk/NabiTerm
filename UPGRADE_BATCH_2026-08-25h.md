# 배치 2026-08-25h — 11 phase, 다섯 분야

v0.1.467 직후. 이번 조사에서 **후보 대부분이 이미 있었다** — 터미널 정규식 검색, 클립보드
이력, known_hosts 관리, 세션 내보내기, 괄호 짝, 저장 시 공백 정리, 탭 이름 변경, 자동
재접속, 진단 로그 보기까지. 그래서 "없는 기능"을 찾는 대신 **있는 것이 제대로 되어 있는가**를
봤고, 거기서 더 큰 것이 나왔다.

## 이번 배치의 축: 상용 제품이라면 없어야 할 결함

### 발견 1 — 번역을 거치지 않은 문자열이 화면에 찍힌다

`tr()`을 통하지 않고 한국어를 그대로 넣은 자리가 여러 곳이다. **영어·일본어로 쓰는 사용자가
한국어를 본다.** 반대 사례도 있다(`replaceui.rs`의 `files`가 영어 고정).

```
controlapp.rs:89   ⏰ 스케줄 등록: {label}
controlapp.rs:90   ✕ 스케줄 오류: {e}
controlui.rs:80    ⚠ 컨텍스트 {pct}% 도달 — 컴팩션 임박
controlui.rs:100   ⏸ {title}: 입력 대기
events.rs:123      ⚠ 서버 리소스 임계: {summary}
scheduler.rs:80    ⏰ 스케줄 '{name}' 10회 연속 실패 — 비활성화
telegrambridge.rs  ✈ 텔레그램 페어링 요청: chat {chat} · 코드 {code}
replaceui.rs:29    {m} × · {f} files
```

우리는 이미 **키가 없는 것**을 잡는 게이트가 있다(`keys_exist`). 그런데 **키를 아예 안 쓰는
것**은 아무도 안 본다. 그 반대편을 막는 게이트를 세운다.

### 발견 2 — 자동 재접속에 물러섬이 없다

`do_reconnect`은 **한 번** 시도한다. 노트북이 깨어나는 중이거나 VPN이 잠깐 끊긴 흔한 경우에
그 한 번이 실패하고 사용자는 모달을 본다. 상용 클라이언트는 몇 번 물러나며 다시 붙는다.

## phase

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| T1 | 셸 | 하드코딩 문자열 8곳을 **i18n으로** 옮긴다 | 발견 1 | 낮음 |
| T2 | 셸 | **재발 방지 게이트** — 사용자 표면에 글자 리터럴이 오면 시험이 잡는다 | 발견 1 | 중(오탐 설계) |
| S1 | SSH | **재접속 백오프** — 여러 번, 물러나며. 진행을 보여 준다 | 발견 2 | 중 |
| S2 | SSH | 재접속 **중단** — 사용자가 언제든 멈춘다 | 자동 동작에는 늘 정지가 있어야 한다 | 낮음 |
| F1 | 파일관리자 | **진단 묶음 내보내기** — 로그+판+환경을 한 파일로(비밀 제외) | "폴더 열어 파일 찾아 보내 주세요"를 없앤다 | 중(비밀 유출 주의) |
| F2 | 파일관리자 | 묶음에 **무엇이 들어가는지 먼저 보여 주고** 내보낸다 | 보내기 전에 알아야 한다 | 낮음 |
| M1 | 메뉴/IA | **보기 메뉴 정리**(10항목) | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | **줄바꿈 열 지정** — 지금은 켜기/끄기뿐 | 코드 규약 폭(80·100·120)에 맞춰 본다 | 중 |
| E2 | 에디터 | 상태바에 **다중 커서 개수** | 지난 배치에 커서를 늘리는 길을 냈는데 몇 개인지 안 보인다 | 낮음 |
| E3 | 에디터 | 다중 커서 **전부 해제** 명령(메뉴·팔레트) | Esc를 모르면 빠져나올 길이 없다 | 낮음 |
| M2 | 메뉴/IA | 새 명령들을 팔레트에 일관 노출 | 드리프트 금지 | 낮음 |

## T2를 어떻게 오탐 없이 만들 것인가

"화면에 나가는 문자열"을 기계가 완벽히 알 수는 없다. 그래서 **좁게** 잡는다.

* 검사 대상은 `notify = Some((...))`처럼 **사용자 알림으로 가는 것이 확실한 자리**와
  `ui.label`/`ui.button` 계열의 **첫 인자**로 한정한다.
* **한글이 든 리터럴만** 막는다(영문은 식별자·기호와 섞여 오탐이 많다).
* 주석은 보지 않는다 — 주석의 한글은 오히려 권장된다.
* 예외가 필요하면 그 줄에 이유를 적게 한다(조용한 예외 금지).
