# 업그레이드 배치 W — 2026-08-26 · **되돌릴 수 없는 것 앞에서 멈춘다**

v0.1.481 이후. 배치 V(LSP)가 다른 세션에서 진행 중이므로 **그 파일들은 건드리지 않는다**
(`editorlsp*.rs` · `lspclient.rs` · `lspservers.rs` · `oncmdready.rs` · 설정의 LSP 묶음).

## 왜 이것인가

세션 표식(운영 · 스테이징 · 개발)을 만들어 두었고 상태 표시줄과 목록에 색까지 칠한다.
그런데 그 표식이 **실제로 막는 것은 붙여넣기 하나뿐이다**:

```rust
// paste.rs
self.pane_tag(pane).is_risky()      // 여러 줄 붙여넣기 확인에만 쓰인다
```

운영 서버에 붙어 `rm -rf /var`를 **손으로 쳐서 엔터**를 누르면 아무 일도 일어나지 않고
그대로 나간다. 표식을 붙인 사람의 기대와 다르다 — 표식을 붙였다는 것은 "여기서는
조심해 달라"는 뜻이다.

브로드캐스트는 더 나쁘다. 여러 창에 **동시에** 나가는데, 그 통로에는 확인이 아예 없다.

같은 결의 구멍이 두 곳 더 있다.

* **원격 삭제**: SFTP에서 지울 때 개수도 경로도 요약해 주지 않는다. `/` 나 홈을 통째로
  고른 실수를 막을 선이 없다.
* **편집기 저장**: 파일을 열어 둔 사이에 밖에서 바뀌어도 모르고 덮어쓴다. 서버 설정
  파일을 편집기와 터미널에서 번갈아 만지는 것이 이 프로그램의 일상인데도 그렇다.

## 확인한 것 (계획 전 grep)

| 확인 | 결과 |
|---|---|
| 위험 명령 판별 | **없음** — `paste.rs`의 여러 줄 경고만 있다 |
| 타이핑 명령 가로채기 | **없음** — `tabsterm.rs`가 바로 `WriteInput` |
| 브로드캐스트 확인 | **없음** |
| SFTP 삭제 확인 | **없음**(개수·경로 요약 없음) |
| 편집기 외부 변경 감지 | **없음**(`mtime` 비교 자체가 없다) |
| 대체 화면 판별 | 있음 — `TermModel::alt_screen()` 재사용 |
| 표식 | 있음 — `pane_tag()` · `SessionTag::is_risky()` 재사용 |

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| W1 | 셸 | **위험 명령 판별기**(순수, 시험) | 나머지가 전부 여기 기댄다 | 낮음 |
| W2 | 셸 | **입력 줄 읽기** — 프롬프트~커서(순수, 시험) | 무엇을 치는 중인지 알아야 한다 | 중 |
| W3 | 셸 | 엔터 가로채기 → 확인창(운영 표식 · 대체화면 아님) | 위 | **높음**(입력 경로) |
| W4 | 셸 | 막은 것을 **말해 준다**(무엇이 왜 걸렸는지) | 조용히 막으면 고장으로 읽힌다 | 낮음 |
| S1 | SSH | **브로드캐스트에도 같은 확인** | 여러 서버 동시 = 가장 위험 | 중 |
| S2 | SSH | 확인창에 **어느 세션·표식**인지 함께 | 창이 여럿이면 헷갈린다 | 낮음 |
| F1 | SFTP | 삭제 전 **개수·경로 요약** 확인 | 위 | 중 |
| F2 | SFTP | **위험한 경로 차단**(`/`·홈·루트 한 단계) | 되돌릴 수 없다 | 낮음 |
| M1 | 메뉴/IA | 설정에 **'보호' 묶음** — 흩어진 확인 설정을 모은다 | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | 저장 시 **외부 변경 감지**(mtime·크기) | 조용한 덮어쓰기 = 데이터 손실 | 중 |
| E2 | 에디터 | 바뀌었으면 **덮어쓰기 · 다시 읽기 · 다른 이름**을 고르게 | 위 | 낮음 |

## 가장 조심할 것 — W3(입력 경로)

**타이핑을 망가뜨리는 것이 이 배치에서 가장 나쁜 결과다.** 확인창이 한 번 헛나오는 것보다
글자가 안 찍히는 것이 훨씬 나쁘다. 그래서 가로채기는 조건을 좁게 건다.

* 표식이 **위험(운영)** 인 세션만 — 표식을 붙이지 않았으면 지금과 완전히 같다.
* **대체 화면(vim · less 등)에서는 아예 보지 않는다** — 거기서는 줄이 명령이 아니다.
* 이번 프레임의 바이트가 **CR로 끝날 때만** — 그 외 타이핑은 손대지 않는다.
* 판별기가 **아니라고 하면 그대로 보낸다.** 막는 것은 좁고 확실한 몇 가지뿐이다.
* 설정으로 끌 수 있다(`terminal.guard_dangerous`, 기본 켬 — 표식이 있어야 동작하므로
  기본을 켜도 표식을 안 쓰는 사람에게는 아무 변화가 없다).

판별기는 **거짓 양성보다 거짓 음성을 택한다.** `rm -rf`처럼 뜻이 분명한 것만 잡고,
애매한 것은 통과시킨다. 잡지 못한 위험은 사용자가 원래 지고 있던 위험이지만, 헛확인은
우리가 새로 만든 짜증이다.

---

## 결과

| # | 결과 |
|---|---|
| W1 | `danger.rs` — 지우기·디스크·전원·덮어쓰기·권한 다섯 갈래. 파이프·`&&`로 이어진 줄은 조각마다 본다. **거짓 음성을 택했다**: `rm -r`(강제 없음)·`git push`·`chmod -R 755 ./www` 는 통과. 시험 10. |
| W2 | `inputline.rs` — 커서 줄에서 프롬프트 뒤~커서 앞을 명령으로 본다. 글자 단위로 자른다(한글). 시험 8. |
| W3 | `guard.rs` + `tabsterm.rs`. **보내는 그 자리에서** 판단한다 — 요청을 남겨 다음 프레임에 처리하면 붙잡아 둔 사이 다음 글자가 먼저 나가 **입력 순서가 뒤집힌다.** 표식은 앱만 알므로 위험 pane 집합을 프레임마다 넘긴다. 시험 4. |
| W4 | `guardui.rs` — 무엇이·어디서·왜를 함께. 기본 단추는 취소, Esc로 닫힌다. |
| S1 | 브로드캐스트도 같은 확인 — **하나라도** 위험 표식이면 묻는다. 붙잡을 때의 대상을 그대로 들고 있다가 그대로 보낸다(그 사이 대상이 바뀌었을 수 있다). |
| S2 | 확인창에 세션 이름·표식(색)·동시 창 수. |
| M1 | 설정 `terminal.guard_dangerous`(기본 켬) — 종료 확인 바로 옆에 뒀다. 표식이 있어야 동작하므로 표식을 안 쓰면 기본을 켜도 변화가 없다. |
| E1·E2 | ⚠️ **범위를 좁혔다** — 아래 참고. `editor_changed_outside()` + `editorconflict.rs`(덮어쓰기·다시 읽기·다른 이름). |
| F1 | ⚠️ **이미 있었다** — `sftpentries.rs`가 개수·폴더 재귀까지 경고하고 있었다. 만들지 않았다. |
| F2 | `sftppath::is_perilous` — 루트·루트 한 단계·홈 자신. 그 아래 평범한 작업 경로는 건드리지 않는다. 시험 2. |

### 두 번 헛짚었다

**E1(외부 변경 감지)은 이미 있었다.** `extwatch.rs`가 2초마다 mtime을 보고, 미수정이면
조용히 다시 읽고 수정 중이면 토스트를 한 번 띄운다. 나는 `nabi-editor`만 grep 하고
"없다"고 적은 뒤 `extchange.rs`와 `EditorDoc.stamp`라는 **평행 구현**을 만들었다.
기준점이 둘이 되면 어긋난 답을 하게 되므로 걷어내고, 기존 `editor_mtimes` 한 곳을 보는
`editor_changed_outside()`로 다시 썼다.

남은 빈틈은 진짜였다 — 토스트는 스쳐 지나가고 **저장은 그대로 덮어썼다.** 그래서 저장
직전에 묻고 세 갈래(덮어쓰기·다시 읽기·다른 이름)를 준다. 어느 것도 내용을 잃지 않는다.

**F1(삭제 확인)도 이미 있었다.** 계획 때 `sftpops.rs`만 보고 없다고 적었는데 확인창은
`sftpentries.rs`에 있었다. 규모 경고(F2)만 더했다.

두 번 다 **grep 범위를 좁게 잡은 것**이 원인이다. 크레이트 하나만 보고 "없다"고
결론 내리지 말 것 — [[feedback-engineering-discipline]]에 이미 적혀 있는 규율이다.

### 게이트가 나를 잡았다

설정 행(`settings.guarddangerous`)을 더하고 **검색 색인에 넣지 않았다.**
`the_index_matches_the_screens`(v0.1.466에 만든 대조 시험)가 소스를 다시 훑어
잡아냈다 — "설정 화면에는 있는데 검색 표에 없다".

손으로 관리하는 짝 목록은 반드시 어긋난다는 것을 만든 사람이 다시 증명한 셈이다
([[feedback-derive-tables-from-source]]). 시험이 없었으면 검색되지 않는 설정이
조용히 하나 늘었을 것이다.
