# 업그레이드 배치 계획 — 2026-08-25 (3회차, v0.1.464~)

배포 창: 11:00은 v0.1.463으로 사용. 이번 배치는 **17:00 창**에 올린다.

## 이번 배치 (12 phase, 5분야)

### ① 셸/터미널
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| T1 | **pane별 스크롤백 상한** | 지금은 전역 하나(`schema.rs scrollback`). AI TUI는 재그리기로 스크롤백을 빨리 먹는데 그 pane만 크게 잡을 수 없다 | 중 / 낮음 |
| T2 | **오래 걸린 명령 완료 알림** | 셸 통합(OSC 133)이 이미 시작·종료를 안다. 자리를 비운 사이 끝난 것을 모르고 지나친다 | 중 / 낮음 |

### ② SSH
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| S1 | **에이전트 포워딩**(`auth-agent-req@openssh.com`) | 로컬 인증은 있고 포워딩만 없다(감사). 원격에서 다시 git/ssh를 쓰는 흐름에 필수. **실제 에이전트로 검증**(스킬 §4 절차) | 상 / 중간 |
| S2 | **연결 실패 진단** | 지금은 오류 문자열 하나. 호스트 도달·포트 개방·인증 중 어디서 막혔는지 알려 주면 사용자가 스스로 푼다 | 중 / 낮음 |

### ③ 파일관리자·SFTP
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| F1 | **원격 파일 미리보기** | 감사: 로컬 미리보기는 있는데 원격은 없다. 편집기로 여는 것 말고 **빠르게 훑는** 길 | 중 / 중간 |

### ④ 메뉴·IA (매 배치 1+ 필수)
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| M1 | **설정 검색(생성기 방식)** | 지난 회차 2번 미룸. 설정 키 107개를 **소스에서 색인 생성**하고 게이트가 어긋남을 잡는다 | 상 / 중간 |
| M2 | **팔레트 최근 사용 우선** | 정적 순서라 자주 쓰는 것이 아래에 묻힌다 | 중 / 낮음 |
| M3 | **줄 수 경고 0으로** | 13→24로 늘었다(이번 세션 신규 코드). 우리 규율이 경고를 쌓아 두면 규율이 아니다 | 중 / 낮음 |

### ⑤ 에디터(nabiPad)
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| E1 | **커서 추가 제스처**(Ctrl+D 다음 항목 / Alt+클릭) | 멀티커서 자료구조는 있는데 **추가하는 방법이 없다**(감사). VS Code의 대표 동작 | 중 / 중간 |
| E2 | **접근성 1단계** | eframe `accesskit`은 이미 켜져 있고 앱 쪽 작업만 비어 있다. 주요 위젯 라벨·포커스 순서 | 상 / 중간 |

## 순서
S1(에이전트 포워딩·실서버 검증) → E1 → T1 → M2 → S2 → T2 → F1 → M1 → E2 → M3(줄 수 정리) → 게이트 → 17:00 배포
