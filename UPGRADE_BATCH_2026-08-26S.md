# 업그레이드 배치 S — 2026-08-26 · **접근성**

v0.1.478 이후.

## 왜 이번엔 기능이 아니라 접근성인가

기능 표면을 다시 훑었다. 메뉴 71 · 팔레트 101 · SFTP 33 · 편집기 80여 · 가져오기 8종
(PuTTY · WinSCP · MobaXterm · FileZilla · XShell · ssh_config · screen · 스니펫). 앞선
배치들에서 결함 클래스(삼켜진 오류 · 조용한 상한 · 미사용 코드)도 훑어 냈다.

그러다 **한 축이 통째로 비어 있는 것**을 봤다:

```text
  고대비 · 접근성 · accessib   →  0건
  색맹 · colorblind            →  0건
  화면 낭독 · screen_reader    →  0건
  대비(contrast) 계산          →  0건
```

터미널은 **색으로 정보를 전달한다.** 오류는 빨강, 성공은 초록, 경로는 파랑. 남성 20명 중
1명은 적록색약이라 그 구분이 흐리거나 사라진다. 우리는 그 사용자에게 아무것도 해 준 적이
없다. 상용 제품이 반드시 갖는 축이고, 우리에게는 **0**이다.

"상용화된 프로그램 이상"을 목표로 한다면 여기가 지금 가장 값이 큰 자리다.

## 확인 결과

| 후보 | 판정 |
|---|---|
| ANSI 16색 팔레트 | `nabi-types::ansi16`에 **고정 상수**. 바꿀 길이 없다 |
| 색 대비 계산 | 없다(WCAG 상대 휘도 계산 자체가 없다) |
| 커서 깜빡임 끄기 | `blink`는 있으나 **설정이 아니다**(늘 깜빡인다) |
| UI 배율 | `set_zoom_factor(1.0)`로 **고정**해 두었다 |
| 색 말고 다른 단서 | 일부만(블록 목록 ✓/✗). 전송·세션 표식은 색뿐 |
| 테마 가져오기 | **이미 있다**(`themeimport`) — 건너뜀 |
| 글꼴 크기 | **이미 있다** — 건너뜀 |

## 계획 (12 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| A1 | 접근성 | **상대 휘도·대비비**(WCAG) 순수 계산 | 나머지가 전부 여기 기댄다 | 낮음 |
| A2 | 접근성 | **색각 친화 ANSI 팔레트** 고를 수 있게 | 적록색약에 빨강/초록이 같아 보인다 | 중 |
| A3 | 접근성 | 테마 **대비 경고** — 읽기 힘든 조합을 알린다 | 배경과 전경을 따로 고르면 생긴다 | 낮음 |
| A4 | 접근성 | **커서 깜빡임 끄기** 설정 | 깜빡임은 광과민·주의력에 부담이다 | 낮음 |
| A5 | 접근성 | **UI 배율** 설정(글꼴과 별개) | 저시력 사용자는 전체가 커야 한다 | 중 |
| A6 | 접근성 | 색 말고 **글자·기호** 단서 더하기(전송·표식) | 색만으로 전달하면 못 읽는 사람이 있다 | 낮음 |
| M1 | 메뉴/IA | 설정에 **접근성 페이지** 신설 | 흩어 두면 아무도 못 찾는다 | 낮음 |
| M2 | 메뉴/IA | 설정 색인·i18n 배선 | 검사기가 잡는 곳 | 낮음 |
| T1 | 셸 | 팔레트 선택을 **렌더 경로에** 잇기 | 고른 것이 실제로 보여야 한다 | 중 |
| E1 | 에디터 | 편집기도 같은 배율·깜빡임 설정을 따른다 | 한 프로그램인데 따로 놀면 안 된다 | 중 |
| F1 | SFTP | 전송 성공/실패에 **기호** 붙이기 | 지금은 색뿐 | 낮음 |
| V1 | 검증 | 팔레트가 실제로 구별되는지 **수치로** 확인 | "친화적"이라는 말을 시험으로 바꾼다 | 낮음 |

## 가장 조심할 것

* **기본값을 바꾸지 않는다.** 지금 화면이 달라 보이면 그건 회귀다. 전부 **끄기가 기본**이고
  켠 사람에게만 달라진다.
* 팔레트를 바꾸는 자리는 `to_rgba` 하나뿐이어야 한다 — 두 곳이 되면 어떤 색은 바뀌고
  어떤 색은 안 바뀐다.
* "색각 친화"는 말이 아니라 **수치**로 보여야 한다. 고른 팔레트의 빨강·초록이 실제로
  구별되는지 시험이 검사한다.

## 결과 (v0.1.479 예정)

### A1 — 대비를 수치로 (`nabi-types/contrast.rs`)

나머지가 전부 여기 기댄다. WCAG 상대 휘도와 대비비 — 사람 눈은 초록에 민감하고 파랑에
둔하므로 RGB 평균이 아니라 가중치를 쓰고, 화면 감마도 되돌린다. 시험 일곱 중 하나가
그 차이를 지킨다(`초록이 파랑보다 여덟 배 넘게 밝다`).

### A2 — 색각 친화 팔레트, 그리고 **내 전제가 반증된 일**

팔레트 셋(기본 · 색각 친화 · 고대비)을 두고, `ansi16` **한 곳**만 갈아 끼우게 했다.
이름색과 인덱스색이 둘 다 그 함수를 지나므로 어떤 색은 바뀌고 어떤 색은 안 바뀌는
일이 생길 수 없다.

시험을 이렇게 썼다:

```rust
  // 기본 팔레트에서 빨강과 초록은 색약 눈에 붙어 보인다
  assert!(ratio < 2.0, "기본 팔레트가 이미 갈린다면 이 배치가 필요 없다");
```

**시험이 나를 반증했다** — 2.34:1로 이미 꽤 갈려 있었다. 까닭은 내 모델이 밝기만 재기
때문이다. 색약 사용자를 실제로 힘들게 하는 것은 **색상 혼동**이고 그건 이 수치로
증명할 수 없다.

그래서 단정을 **견주기**로 바꿨다: 색각 친화 팔레트가 기본보다 더 크게 갈라 놓는가
(2.34 → 4.01). 증명할 수 있는 것만 시험하고, 색상 쪽은 Okabe-Ito가 색각 이상자를
대상으로 검증해 공개한 값을 그대로 쓰는 것으로 대신한다 — **우리가 지어내지 않았다는
것이 근거다.**

### A3 — 대비 경고

전경/배경을 따로 고를 수 있으니 읽을 수 없는 조합이 쉽게 만들어진다. 설정 화면에서
**그 자리에 수치와 함께** 알린다(`4.5:1` 미만이면 경고). "낮습니다"만으로는 얼마나
고쳐야 하는지 알 수 없다.

### A6 — 기호 단서, 그리고 **덜 만든 일**

색만으로 뜻을 나르는 곳을 훑었다. 그런데 대부분은 **이미 기호를 함께 쓰고 있었다** —
전송 큐 ✓/✗, 도달 확인 표시, 종료 코드 칩. 색만 쓰던 곳은 서버 통계 경고 하나였다.

그래서 `WARN` 하나만 두고 성공 기호(`OK`)는 **만들지 않았다.** 쓸 자리가 없는 상수를
미리 만들어 두면 다음 사람이 "왜 안 쓰지?"를 묻게 된다. 이 발견 자체가 결과다 —
이 프로그램은 접근성의 이 부분을 이미 대체로 지키고 있었다.

### M1 — 접근성 페이지

커서 깜빡임(`cursor_blink`)과 UI 배율(`ui_scale`)은 **이미 있었다.** 다만 '모양'
페이지에 흩어져 있었다. 색약 사용자가 "이 프로그램은 나를 위한 게 없다"고 판단하는
데는 설정 화면을 한 번 훑는 시간이면 충분하다.

옮기지 않고 **모았다** — 원래 자리에도 그대로 있고 접근성 페이지에서도 보인다. 배율은
'모양' 페이지가 쓰는 그 함수를 그대로 부른다(눈금·범위·기본값이 어긋나지 않게).

### 계획 정정

| 계획 | 실제 |
|---|---|
| A4 커서 깜빡임 끄기 | **이미 있었다** → 접근성 페이지에 모으는 일로 바뀜 |
| A5 UI 배율 | **이미 있었다**(`ui_scale_row`) → 그 함수를 재사용 |
| A6 기호 단서 | 대부분 **이미 하고 있었다** → 한 곳만 보태고 나머지는 안 만듦 |

새로 만든 것은 A1 · A2 · A3 · M1 · T1(팔레트 배선) · A6(한 곳) · M2(색인)다.

### 드리프트 검사기가 또 잡았다

새 설정 화면을 만들자 `the_index_matches_the_screens`가 세 번 걸렸다 — 검사기에 새
파일을 안 알림 → 하위 함수(`contrast_note`)를 안 알림 → 색인에 키 하나 빠짐. 손으로
관리하는 표는 반드시 어긋난다는 것을 매번 확인한다.

## 검증

```text
clippy 0 · lines 신규 초과 0 · 한 줄 길이 초과 0 · prerelease 0
```
