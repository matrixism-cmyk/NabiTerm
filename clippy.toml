# clippy 설정. MSRV를 알려주면 최신 문법을 제안하는 린트가 우리가 못 쓰는 API를 권하지 않는다.
msrv = "1.87"

# 인자 7개까지는 허용. 렌더 함수들이 좌표·색·폰트를 함께 받는 경우가 많은데,
# 그때마다 #[allow]을 붙이면 정작 진짜 과한 시그니처를 못 알아본다.
too-many-arguments-threshold = 8
