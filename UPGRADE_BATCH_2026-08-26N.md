# 업그레이드 배치 N — 2026-08-26

v0.1.473 이후. **이번엔 검색어를 여러 벌 쓰고 파일 목록도 눈으로 훑었다** — 지난 배치에서
같은 실수를 세 번(`box_select`·`copy_suffixed`·`just_drained`) 한 뒤 규율에 넣은 것이다.

## 이번에 확인한 것

각 후보를 **동의어 서너 개**로 찾고, `ls crates/*/src`로 파일 이름도 훑었다.

| 후보 | 판정 |
|---|---|
| 원격→원격 복사 | `remote_copy`·`server_copy`·`SftpCopy`·`duplicate_remote` **전부 없음** |
| 원격 명령 실행 | `SftpExec`·`RemoteExec`·`exec_channel`·`ExecCommand` **전부 없음** |
| 명령 출력 접기 | `fold_output`·`collapse_block` **없음** |
| 세션 표식으로 거르기 | `filter_by_tag`·`tag_filter` **없음** |
| 재귀 권한 변경 | 프로토콜에 `SftpChmodRecursive`가 **있다** — UI에 닿는지 확인 필요 |
| 여러 파일 바꾸기·북마크·탭 색·스니펫 | **이미 있다**(만들지 않는다) |

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| F1 | SFTP | **서버 안에서 복사** — 원격→원격 | FileZilla·WinSCP의 기본. 지금은 받았다 다시 올려야 한다 | 중 |
| F2 | SFTP | 복사 이름 규칙(사본 붙이기)을 원격에도 | 같은 폴더 복사는 이름이 겹칠 수밖에 없다 | 낮음 |
| F3 | SFTP | **재귀 권한 변경이 UI에 닿는지** 확인·노출 | 프로토콜에 있는데 부르는 곳이 없으면 죽은 코드다 | 낮음 |
| S1 | SSH | **고른 파일에 원격 명령** — 압축·해제·검사 | 터미널로 건너가 경로를 다시 치게 된다 | 중 |
| S2 | SSH | 원격 명령 결과를 창으로 보여 주기 | 실행만 하고 결과를 못 보면 반쪽 | 중 |
| T1 | 셸 | **명령 출력 접기** — 긴 출력을 접어 둔다 | 블록은 이미 있다(OSC 133). 접는 길만 없다 | 중 |
| T2 | 셸 | 접은 상태를 화면 이동에도 유지 | 접었는데 스크롤하면 풀리면 안 된다 | 중 |
| M1 | 메뉴/IA | 이번 기능의 노출 정리 | 매 배치 IA phase | 낮음 |
| M2 | 메뉴/IA | **세션을 표식으로 거르기** — 운영만 보기 | 세션이 늘면 목록에서 찾는 것이 일이 된다 | 낮음 |
| E1 | 에디터 | 원격 명령 결과를 편집기로 보내기 | 긴 결과는 편집기에서 봐야 한다(찾기·저장) | 낮음 |
| E2 | 에디터 | 접기 상태를 탭 전환에도 유지 | `Folds`가 `EditBuf`에 있어 될 법한데 확인 필요 | 낮음 |

## 가장 조심할 것 — 원격 명령(S1)

**서버에서 명령을 실행하는 기능은 위험하다.** 그래서 좁게 만든다.

* 사용자가 **직접 고른 명령**만 — 목록에서 고르고, 무엇이 실행될지 **먼저 보여 준다**.
* 파일 이름은 **인용해서** 넘긴다. 이름에 공백·따옴표·`;`가 있으면 그대로 명령이 된다.
* 결과는 **읽기 전용**으로 보여 준다. 실행 중 취소할 수 있어야 한다.

붙여넣기 확인창을 만든 것과 같은 생각이다 — 위험한 일일수록 **하기 전에 보여 준다**.

---

## 진행 (v0.1.474 예정)

### F3은 만들지 않았다 — 이미 완비돼 있었다

`SftpChmodRecursive`가 프로토콜에 있는 것을 보고 "부르는 곳이 없을지도" 의심했는데,
`sftpops.rs:63`이 보내고 `sftpperms.rs`에 재귀 선택 UI까지 있었다. 계획에서 뺐다.

### F1 — 서버 안에서 복사

지금까지 서버 안에서 파일을 복사하려면 **받았다가 다시 올려야** 했다. 회선을 두 번 타는
것에 더해, 큰 파일에서는 임시 파일이 디스크 한 벌을 차지하고 중간에 끊기면 찌꺼기가 남는다.

`SftpFs::copy_remote`는 서버에서 조각을 읽어 **곧바로 서버로 되쓴다**. 회선은 여전히 두 번
타지만(SFTP v3에는 "서버 안에서 옮겨라"가 없다) **디스크는 건드리지 않는다.**

설계에서 신경 쓴 것:

- 대상을 못 열면 **원본 손잡이를 닫고** 나간다 — 서버의 열린 파일 수는 한정돼 있다.
- 실패하면 **반쯤 쓰다 만 파일을 지운다** — 남으면 성공한 복사로 오해한다.
- 취소는 전송과 **같은 깃발**을 쓴다(멈추라면 여기서도 멈춘다).
- FTP는 **못 한다고 말한다**. 되는 척 받았다 올리면 왜 이 파일만 느린지 알 수 없다.
- 사유는 오케스트레이터가 **i18n 키로** 돌려준다 — 그 층은 화면 언어를 모른다.
- 사본 이름은 로컬 브라우저의 복제와 **같은 규칙**(`copy_suffixed`)을 쓴다. 두 곳이 다르게
  지으면 사용자가 규칙을 두 번 배워야 한다.
- 큐(`push_xfer`)를 그대로 타므로 **진행률·순서 변경·취소·재시도**가 공짜로 따라온다.

### ⚠️ 실서버 검증이 남았다

스킬 §4: "전송·SFTP를 건드렸으면 실서버 검증까지 해야 통과다. 인프로세스 시험은 우리가
만든 서버라 우리가 기대하는 대로만 답한다 — 실제로 두 번 다 실서버에서만 결함이 나왔다."

인프로세스 시험 3건(복사 왕복·없는 원본·빈 파일)은 통과했지만, **실 OpenSSH에서 한 번 더
돌려야 배포 자격이 된다.** 이번 배치는 여기까지 하고 실서버 검증 후 배포한다.
