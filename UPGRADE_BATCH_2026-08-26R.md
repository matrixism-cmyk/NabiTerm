# 업그레이드 배치 R — 2026-08-26

v0.1.477 이후.

## 이번 찾는 법 — 결함 사냥 먼저, 그다음 기능 표면

지난 배치들에서 가장 값진 것은 기능이 아니라 **결함 클래스**였다(조용한 상한, 삼켜진
오류, 만들어 놓고 안 쓰는 코드). 그래서 이번에는 그것부터 훑었다.

| 훑은 것 | 결과 |
|---|---|
| `Err(_) => 기본값` 9곳 | 전부 **까닭이 적혀 있었다**(에이전트 없음=키 없음 등). 남은 하나가 배치 Q에서 고친 호스트키다 |
| 상한·자름 146곳 | `read_capped`가 `(바이트, 잘렸는가)`를 돌려주는 식으로 **이미 정직했다** |
| 크레이트 밖 미사용 함수 | 전부 크레이트 **안에서** 쓰이고 있었다(내 걸러내기가 틀렸다) |

결함이 잘 안 나온다는 것 자체가 결과다. 그래서 **사용자가 보는 기능 표면**(메뉴 71개 ·
팔레트 101개 · SFTP 33개)을 훑어 없는 것을 찾았다.

## 확인 결과

| 후보 | 판정 |
|---|---|
| **공개키를 서버에 설치**(ssh-copy-id) | `copy_id`·`authorized_keys 설치` **없다.** 키는 만들어 주고 붙여넣기는 손으로 |
| diff 결과에서 **한쪽 적용** | `merge` 없음 — 견주기만 되고 고치는 길이 없다 |
| 여러 서버에 **파일** 배포 | 동시 입력은 글자만. 파일은 없다 |
| 최근 목록 `take(10)` | 스무 개를 기억하고 **열 개만** 보여 준다(조용한 자름) |
| 전송 후 원격 권한 | **이미 있다**(`uploadmode::mode_for`) — 건너뜀 |
| 일괄 이름 변경·동기화·비교 | **이미 있다** — 건너뜀 |

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| S1 | SSH | **authorized_keys 줄 만들기·중복 검사**(순수) | 설치의 옳고 그름이 여기서 갈린다 | 중 |
| S2 | SSH | **공개키를 서버에 설치**(exec 통로 재사용) | 키 설정의 가장 큰 마찰 | **높음** |
| S3 | SSH | 설치할 키 **고르기**(파일·에이전트·방금 만든 것) | 어느 키인지 모르면 못 쓴다 | 중 |
| E1 | 에디터 | diff에서 **한 덩이 적용**(순수 파서) | 견주기만 되고 고치는 길이 없다 | 중 |
| E2 | 에디터 | 적용 결과를 **미리 보고** 저장 | 남의 파일을 덮는 일이다 | 중 |
| F1 | SFTP | 원격 목록 필터를 **경로까지** | 지금은 이름만 본다 | 낮음 |
| T1 | 셸 | 최근 목록의 **조용한 자름**을 없앤다 | 스무 개 기억하고 열 개만 보여 준다 | 낮음 |
| T2 | 셸 | 블록 목록에 **찾기 칸** | 명령이 많으면 목록도 길다 | 낮음 |
| M1 | 메뉴/IA | 새 기능 노출 + SSH 도구 묶음 정리 | 매 배치 IA phase | 낮음 |
| M2 | 메뉴/IA | 설정·색인 드리프트 배선 | 검사기가 잡는 곳 | 낮음 |
| V1 | 검증 | ssh-copy-id **실서버 검증** | 남의 서버 파일을 고치는 기능이다 | 높음 |

## 가장 조심할 것 — 키 설치(S1~S3)

**남의 서버의 `~/.ssh/authorized_keys`를 고치는 일이다.** 잘못하면 그 서버에 못 들어간다.

* **덮어쓰지 않는다.** 언제나 덧붙이기(`>>`)다.
* **이미 있으면 넣지 않는다.** 같은 키가 두 줄이 되면 지울 때 헷갈린다.
* **권한을 맞춘다.** `~/.ssh`는 700, `authorized_keys`는 600 — 아니면 sshd가 거부한다.
* 실행 전에 **무엇이 실행되는지 보여 준다**(원격 명령과 같은 규칙).
* 키 글자는 **인용한다** — 이미 있는 `remotecmd::shell_quote`를 쓴다.

## 결과 (v0.1.478 예정)

### S1~S3 — 공개키를 서버에 설치 (이번 배치의 핵심)

키 만들기는 있었는데(`keygen`) **그다음 한 걸음**이 없었다. 공개키를 복사해 서버에
붙여 넣고 권한까지 손으로 맞추는 일 — SSH 설정에서 가장 자주 막히는 자리다.

남의 서버 파일을 고치는 일이라 규칙을 좁게 못 박고 순수 함수로 시험했다(10건):

- **덧붙이기만 한다.** `>` 한 글자면 남의 키가 전부 날아간다. 시험이 이것만 본다.
  (처음 쓴 시험은 순진했다 — `">>"`는 `"> "`를 품어서 글자 찾기로는 못 가른다.
  꺾쇠가 **딱 둘이고 붙어 있는지**를 보도록 고쳤다.)
- **이미 있으면 넣지 않는다.** 주석 차이는 무시한다(`user@laptop`과 `user@새노트북`은
  같은 키다). 그래서 먼저 읽고, 없을 때만 넣는다 — 한 줄로 줄일 수도 있지만 그러면
  "이미 있었다"와 "새로 넣었다"를 구별해 말할 수 없다.
- **권한을 맞춘다**(700/600). 안 맞추면 "넣었는데 안 되는" 가장 흔한 실패가 된다.
- 각 단계는 `&&`로 잇는다 — 폴더를 못 만들었는데 덧붙이면 안 된다.

새 연결도 새 인증도 필요 없다. 배치 O의 exec 통로를 그대로 써서 **붙어 있는 SFTP
세션의 SSH 연결**에 채널 하나를 더 연다.

### V1 — 실서버 검증에서 또 걸렸다

이 머신의 sshd는 Windows OpenSSH라 기본 셸이 `cmd.exe`다. 그런데 내가 쓴 건너뛰기
가드가 **약했다**:

```text
  uname -s  →  "The syntax of the command is incorrect."   ← 비어 있지 않다!
```

"비어 있지 않으면 POSIX"로 판정해 시험이 통과해 버릴 뻔했다. 진짜 uname 답은 한
낱말이므로(`Linux`·`Darwin`·`MINGW64_NT-10.0`) **낱말 수와 길이**로 가르도록 고쳤다.
배치 O에서 겪은 것과 같은 함정이고, 같은 자리에서 두 번째다.

그래서 이 머신에서는 설치 경로를 끝까지 볼 수 없다. 대신 **진짜 리눅스 셸**에서
임시 HOME(`mktemp -d`)을 만들어 코드가 만든 명령을 그대로 돌려 확인했다(사용자의
`~/.ssh`는 건드리지 않았고, 임시 폴더는 곧바로 지웠다):

```text
  nabi-copyid-ok
  파일:  ssh-ed25519 AAAA... nabi-copyid-test      ← 정확히 한 줄
  권한:  700 .ssh · 600 authorized_keys           ← sshd가 받아들이는 값
  두 번 넣은 뒤 줄 수: 2                           ← 덧붙이기 확인(덮어썼다면 1)
```

마지막 줄이 중요하다. 명령 자체는 **묻지 않고 덧붙인다** — 그래서 앱이 넣기 전에
반드시 `already_present`로 확인한다.

### E1·E2 — diff에서 한쪽 글 만들기

비교는 되는데 **고치는 길이 없었다.** 덩이(hunk)를 하나씩 적용하는 편집기도 많지만,
그러려면 diff 글과 원본이 정확히 짝인지 매번 확인해야 한다(사용자가 diff 문서를 손으로
고쳤을 수도 있다). 대신 **전부 한쪽으로**만 만든다 — 좁지만 틀릴 수 없는 길이고, 결과를
새 문서로 열어 **눈으로 보고 저장**한다.

시험이 결함을 잡았다: 표시가 두 글자인데 **한 글자만 떼고** 있었다. 알맹이 앞에 공백이
남는데, 한글 줄에서는 눈으로 알아채기 어렵다.

### T1 — 조용한 자름을 없앴다

최근 경로를 **스물 기억하고 열만** 보여 주고 있었다. 나머지 열은 화면에 안 나오는데
설정 파일에는 남는다 — 사용자에게는 사라진 것이고 디스크에는 남은 셈이다. 기억하는
수를 열로 낮춰 **기억 = 보여 줌**으로 만들었다.

### T2 — 블록 목록에 찾기 칸

명령이 쌓이면 목록도 길어진다. 실패만 보기와 나란히 이름으로 좁힌다.

## 이번 배치가 알려 준 것

결함 사냥(삼켜진 오류·조용한 상한·미사용 코드)이 **거의 빈손이었다.** 지난 배치들에서
그 부류를 훑어 낸 결과다. 남은 자름 하나(최근 목록)를 없애고, 그다음부터는 기능 표면을
훑는 쪽이 더 나왔다.

## 검증

```text
clippy 0 · lines 신규 초과 0 · 한 줄 길이 초과 0 · prerelease 0 · unsafe 근거없음 0
```
