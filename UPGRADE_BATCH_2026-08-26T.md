# 업그레이드 배치 T — 2026-08-26 · **비밀이 디스크에 남는 문제**

v0.1.479 이후.

## 어떻게 찾았나

지난 배치에서 "빠진 품질 축"을 찾는 방식이 통했다(접근성). 이번에도 축부터 훑었고,
그러다 **내가 지난 배치에 만든 것**이 걸렸다.

배치 Q에서 세션 로그 자동 시작을 만들며 릴리스 노트에 이렇게 적었다:

> 기본은 꺼짐입니다. 터미널에는 비밀번호도 지나갑니다 — 묻지 않고 디스크에 남길 일이 아닙니다.

맞는 말인데, **켠 사람에게는 아무 보호가 없다.** 그리고 훑어보니 더 나쁜 것이 있었다.

## 확인 결과

| 무엇이 디스크에 남나 | 가려지나 |
|---|---|
| **명령 히스토리**(`config.toml`의 `cmd_history`) | **아니다.** 명령 전문이 평문 |
| **세션 로그**(배치 Q에서 자동 시작 추가) | **아니다.** 터미널 출력 그대로 |
| 지원 번들 | 그렇다(`supportbundle::redact`) |
| 연결 기록(`connections.json`) | 해당 없음(host·user·시각만) |
| 자격 증명 볼트 | Argon2id + AES-256-GCM |

`redact`는 **한 곳에서만** 쓰이고 있었다. 만들어 놓고 다른 데 안 쓰는 코드 —
메모리에 적어 둔 결함 부류 그대로다.

명령 히스토리가 특히 나쁘다. 이런 것이 그대로 남는다:

```text
  curl -H "Authorization: Bearer sk-live-..." https://api...
  mysql -u root -pP@ssw0rd
  export OPENAI_API_KEY=sk-...
```

설정 파일은 백업·동기화·지원 문의로 밖에 나가기 쉬운 파일이다.

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| S1 | 보안 | `redact`를 **공용 모듈**로 승격 | 한 곳에만 있으면 나머지가 못 쓴다 | 낮음 |
| S2 | 보안 | 가리기 규칙 **강화**(`-p비번`·`?token=`·긴 토큰) | 지금 규칙은 `키: 값` 꼴만 본다 | 중 |
| S3 | 보안 | **명령 히스토리**에 적용(기록 시점) | 설정 파일이 밖으로 나간다 | 중 |
| S4 | 보안 | **세션 로그**에 적용(설정, 기본 켬) | 켠 사람에게 보호가 없었다 | 중 |
| S5 | 보안 | 이미 저장된 기록도 **불러올 때** 가린다 | 어제 남은 토큰은 오늘도 토큰이다 | 중 |
| T1 | 셸 | 명령 기록 화면에 **가려짐 표시** | 왜 다르게 보이는지 알아야 한다 | 낮음 |
| M1 | 메뉴/IA | 보안 설정을 한자리에(가리기 스위치) | 흩어 두면 못 찾는다 | 낮음 |
| M2 | 메뉴/IA | 설정 색인·i18n 배선 | 검사기가 잡는 곳 | 낮음 |
| E1 | 에디터 | 저장 전 **비밀 경고**(붙여넣은 키가 든 문서) | 실수로 커밋되는 흔한 사고 | 중 |
| F1 | SFTP | 업로드 전 같은 경고 | 서버에 올리면 되돌리기 어렵다 | 중 |
| V1 | 검증 | 진짜 비밀 꼴로 **시험** | 규칙은 시험 없이는 믿을 수 없다 | 낮음 |

## 가장 조심할 것

* **넉넉하게 가린다.** 지나치게 가려 진단이 조금 불편한 쪽이, 토큰 하나가 새는 쪽보다
  낫다. 되돌릴 수 없는 것은 유출이지 불편이 아니다.
* 그렇다고 **아무 줄이나 지우면** 기록이 쓸모없어진다. 무엇이 지워졌는지는 보여야 한다.
* 가리기는 **저장 직전**에 한다. 화면에서만 가리면 파일에는 그대로 남는다.

## 결과 (v0.1.480 예정)

### S1 — 가리기를 밖으로 (`redact.rs`)

`supportbundle::redact`가 한 곳에만 있었다. **한 곳에만 있는 안전장치는 나머지를 지키지
못한다.** 밖으로 꺼내 세 곳(지원 번들 · 명령 기록 · 세션 로그)이 같은 규칙을 쓴다.

### S2 — 규칙 강화, 그리고 **옛 시험이 새 규칙의 빈틈을 잡았다**

옛 규칙은 열쇠말이 있으면 **줄 전체**를 지웠다. 그러면 `curl -H Authorization=...`이
통째로 사라져 무슨 명령이었는지도 잃는다. 그래서 조각 단위로 바꿔 값만 지우게 했다.

그런데 옮기는 과정에서 지원 번들의 옛 시험이 실패했다:

```text
  left:  "password: x"
  right: "password: [redacted]"
```

**로그에서 가장 흔한 꼴**(구분자 뒤에 띄어 쓴 값)이 새 규칙에서 새고 있었다. 내
조각 규칙은 `password:`를 열쇠말로 못 알아봤다(뒤의 콜론 때문에). 옛 시험이 없었으면
"규칙을 강화했다"고 보고하면서 실은 흔한 경우를 놓칠 뻔했다.

새로 넣은 것: 공개된 토큰 접두사(`sk-`·`ghp_`·`AKIA`·`AIza`…), MySQL 꼴 `-p비번`,
띄어 쓴 값(`--token abc`), 구분자로 끝나는 열쇠말(`password: x`).

지키기로 한 선: **`-p` 하나만 있는 것은 비밀이 아니다**(대화식으로 묻는 꼴), 짧은
글자는 접두사가 같아도 토큰이 아니다(`sk-1`).

### S3·S4 — 저장 직전에 가린다

**화면에서만 가리면 파일에는 그대로 남는다.** 그래서 기록하는 그 줄에서 가린다.

명령 히스토리가 특히 급했다 — 설정 파일은 백업·동기화·지원 문의로 밖에 나가기 쉽다.
세션 로그는 배치 Q에서 내가 자동 시작을 만든 것이고, 그때 "터미널에는 비밀번호도
지나갑니다"라고 적어 놓고 **켠 사람에게는 아무 보호도 두지 않았다.**

### S5 — 어제 남은 토큰은 오늘도 토큰이다

가리기가 생기기 전에 쌓인 기록에는 비밀이 그대로 있다. 불러올 때 한 번 훑는다.
**꺼 놓았으면 손대지 않는다** — 끈 사람은 원문을 원하는 것이고 그 뜻을 뒤집지 않는다.

### T1·M1·M2 — 보이게, 그리고 끌 수 있게

명령 기록 창에 `비밀은 가려짐`이 뜬다. 모르면 기록이 깨진 줄 안다. 설정(원격 연결
페이지)에서 둘 다 끌 수 있고, 기본은 **켬**이다.

### 계획에서 뺀 것

E1(편집기 저장 전 비밀 경고)과 F1(업로드 전 경고)은 **이번에 하지 않았다.** 둘 다
"경고를 띄운다"는 별개의 상호작용이고, 규칙이 막 바뀐 참이라 오탐이 곧바로 사용자를
귀찮게 한다. 규칙이 실사용에서 한 판 검증된 뒤에 붙이는 편이 낫다.

## 검증

```text
clippy 0 · lines 신규 초과 0 · 한 줄 길이 초과 0 · prerelease 0 · unsafe 근거없음 0
```
