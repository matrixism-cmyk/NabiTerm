# 배치 2026-08-25k — 11 phase, 다섯 분야

v0.1.470 직후. 먼저 [[feedback-use-what-is-built]] 규칙대로 "호출 0회"를 다시 훑었는데
**하나도 없었다** — 지난 두 배치에서 다 이었다. 규칙이 값을 했다.

그래서 이번에는 **정확성과 손실 방지**를 본다. 이 부류는 사용자가 겪기 전에는 눈에 안 띈다.

## 찾은 결함

**줄 끝이 섞인 파일을 알려 주지 않는다.** `detect_eol`은 CRLF/LF/CR 중 **하나**만 고르고
상태바에 그것만 보인다. 섞였다는 사실이 화면 어디에도 없다. EmEditor·Notepad++는 알려 준다.

⚠️ **구현하며 범위를 좁혔다.** 처음에는 "저장하면 온 파일이 바뀐다"고 적었는데, 코드를 읽어
보니 **일반 문서는 안전했다** — `doc.text`는 정규화 없이 원본 그대로 들고 있고
`format_on_save`도 줄 끝을 건드리지 않는다. 실제로 통일되는 것은 **rope(대용량) 경로뿐**이다
(`editbuf.rs`가 `normalize_lf`로 LF로 폈다가 `to_bytes`에서 `eol` 하나로 되돌린다).
그래서 그 경로에서는 **정규화하기 전에** 세어 둔다. 처음 주장을 그대로 두면 없는 결함을
고쳤다고 보고하게 된다.

**전송이 여러 개 실패해도 하나씩 눌러야 한다.** 큐에 재시도 단추가 있지만 항목마다 하나씩이다.
회선이 끊겼다 돌아오면 스무 개가 한꺼번에 실패하는데, 스무 번 눌러야 한다.

**로그에서 원하는 줄만 보는 길이 없다.** 찾기(Ctrl+F)는 일치를 **강조**하고 오갈 뿐,
`grep`처럼 **맞는 줄만 남겨** 보는 것은 못 한다. 로그를 볼 때 가장 자주 하는 일이다.

**심볼릭 링크는 표시만 되고 따라갈 수 없다.** `is_link`까지 오는데 목록에서 누르면
아무 일도 없다(대상이 폴더인지 파일인지 모른다).

## phase

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| T1 | 셸 | **출력 걸러보기** — 맞는 줄만 남겨 본다(정규식 포함) | 위 | 중 |
| T2 | 셸 | 걸러본 결과 **복사·저장**, 원래 줄번호 유지 | 걸러 놓고 못 가져가면 반쪽 | 낮음 |
| S1 | SSH | **연결 유지 시간 표시**(`ssh_connect_time`은 있는데 안 보인다) | 얼마나 붙어 있었는지 | 낮음 |
| S2 | SSH | 세션별 **마지막 실패 이유 기억**·목록에 표시 | 진단(v0.1.466)과 이어진다 | 낮음 |
| F1 | 파일관리자 | **실패한 전송 전부 재시도** | 위 | 낮음 |
| F2 | 파일관리자 | **심볼릭 링크 따라가기**(대상 종류를 물어 이동/열기) | 위 | 중(순환 링크) |
| M1 | 메뉴/IA | **상태 표시줄 정리** — 항목이 늘었다(재접속·여유공간·통계) | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | **줄 끝 섞임 감지** — 열 때 알리고 개수를 보여 준다 | 위 | 낮음 |
| E2 | 에디터 | 저장 시 **섞인 것을 말없이 통일하지 않는다**(선택하게) | 위 — 조용한 손실 | 중 |
| E3 | 에디터 | 상태바에 줄 끝 표시를 **섞임까지** 나타낸다 | 지금은 하나만 보여 준다 | 낮음 |
| T3 | 셸 | 걸러보기를 팔레트·메뉴에 일관 노출 | 드리프트 금지 | 낮음 |

## 가장 조심할 것 — E2(조용한 변경)

지금 동작은 **섞인 파일을 열었다가 저장하면 온 파일의 줄 끝이 바뀐다.** 사용자는 한 줄만
고쳤다고 생각하는데 diff는 전부다. 고치되 **기존 동작을 깨지 않아야** 한다 — 섞이지 않은
파일은 지금과 똑같이 동작해야 하고, 섞인 파일에서만 묻는다.

---

## 결과 (v0.1.471, 11/11 완료)

| # | 결과 |
|---|---|
| T1·T2·T3 | **별도 창을 만들지 않았다.** `findall`(모든 창에서 찾기)이 이미 같은 일을 하고 있어서, 새 창 대신 거기에 제외(뒤집기)·이 창만·복사/저장을 붙였다. 계획대로 `outfilter.rs` 를 따로 만들었다가 지웠다 — 같은 질문에 두 창이 다르게 답하면 어느 쪽을 믿어야 할지 알 수 없다. |
| S1 | 이미 있었다(`ssh_connect_time` + `human_uptime`). 다만 **서버 통계가 있을 때만** 보이도록 묶여 있어서, 통계가 비면 유지 시간까지 사라졌다. 그 결합만 끊었다. |
| S2 | `lastfail.rs` — 접속 정보(`SessionKind`)를 열쇠로 마지막 실패를 기억하고 사이드바에 ⚠ 로 남긴다. 붙으면 지운다. 곁들여 `classify` 가 `"auth failed"` 를 놓치던 것을 고쳤다(가장 흔한 실패가 미분류로 떨어졌다). |
| F1 | 큐 머리에 `↻{개수}` — 실패가 둘 이상일 때만 낸다(하나면 그 줄에 이미 있다). |
| F2 | `linkres.rs` + `FileKind::LinkDir`. 폴더 링크는 `is_dir`·`is_link` 가 **둘 다** 참이라 들어갈 수 있으면서 링크로 보인다. 목록당 100개까지 8개씩 묶어 확인하고, 넘친 수는 로그에 남긴다. |
| M1 | `statusfit.rs` — 폭에 따라 4단계. 접힌 칩은 버리지 않고 `⋯` 안에 넣는다(없애는 것과 접는 것은 다르다). |
| E1·E3 | `eolmix.rs` — CRLF 를 하나로 세고, 섞였으면 상태바에 `⚠ CRLF 1 · LF 3`. |
| E2 | **범위를 좁혔다.** 처음 계획은 "저장하면 온 파일이 바뀐다"였는데 코드를 읽어 보니 일반 문서는 안전했고(`doc.text` 가 원본을 지킨다), 실제로 통일되는 것은 **rope(대용량) 경로뿐**이었다. 그래서 묻는 대화상자 대신 그 경로의 상태바에 저장 전 경고를 뒀다. |

### 곁다리로 고친 것

- `session.rs` 의 에이전트 포워딩 실패 문구가 **CR 없이 LF만** 내보내 계단으로 밀렸다.
  과거 이스케이프 붕괴가 소스에 진짜 개행으로 남아 있던 것 — 워크스페이스를 훑어
  같은 잔해를 찾았고, 나머지는 전부 시험 입력이라 동작에 영향이 없었다.
- 무인 설치(`/NOLAUNCH`, 명령줄 `/ALLUSERS`) — 사용자 요청.
