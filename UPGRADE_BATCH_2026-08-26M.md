# 업그레이드 배치 M — 2026-08-26

v0.1.472 이후. 계획 전 grep으로 확인해 **이미 있는 것은 뺐다**: 선택 시 자동 복사·미니맵·
자동 저장·서버 지연 표시·터미널 사각 선택은 전부 구현돼 있었다.

## 이번 배치의 중심

배치 L에서 미룬 **에디터 열(사각) 선택**이 이번 배치의 큰 항목이다. 흥미로운 사실 하나 —
**터미널에는 이미 사각 선택이 있다**(`selection.rs`의 `rect: bool`). 편집기에만 없다.
같은 프로그램 안에서 같은 조작이 한쪽에서만 되는 것은 그 자체로 결함에 가깝다.

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| E1 | 에디터 | **열(사각) 선택 — 읽기까지** | 터미널엔 있는데 편집기엔 없다. VS Code·EmEditor의 기본기 | 높음 |
| E2 | 에디터 | **전체 접기 / 전체 펴기** | `Folds`에 `toggle`·`unfold_containing`만 있다 | 낮음 |
| E3 | 에디터 | **바꾸기 미리보기** — 몇 곳이 어떻게 바뀌는지 먼저 | 되돌리기가 있어도 "먼저 보는 것"과는 다르다 | 중 |
| F1 | SFTP | **서버 안에서 복사** — 원격→원격 | FileZilla·WinSCP의 기본. 지금은 받았다 다시 올려야 한다 | 중 |
| F2 | SFTP | **고른 파일에 원격 명령** — 압축·해제·권한 등 | 터미널 창으로 건너가 경로를 다시 치게 된다 | 중 |
| S1 | SSH | **접속 이력** — 언제 어디에 붙었고 얼마나 있었나 | 세션별 마지막 실패는 남지만 성공 이력이 없다 | 낮음 |
| T1 | 셸 | **분할 크기 균등화** | 나누다 보면 비뚤어지는데 되돌릴 길이 없다 | 낮음 |
| T2 | 셸 | **붙여넣기 미리보기** — 확인창에 실제 내용 | 지금은 "여러 줄입니다"만 말하고 무엇인지는 안 보여 준다 | 낮음 |
| M1 | 메뉴/IA | 이번 기능들의 노출 정리 + 편집 메뉴 재점검 | 매 배치 IA phase | 낮음 |
| M2 | 메뉴/IA | **터미널·편집기의 사각 선택을 같은 이름으로** | 같은 조작이 두 이름이면 배운 것이 옮겨가지 않는다 | 낮음 |
| E4 | 에디터 | 접기 상태를 파일 다시 열 때까지 유지 | 접어 놓고 다른 파일 봤다 오면 다 펴져 있다 | 중 |

## 가장 조심할 것 — E1

선택 모델이 `(시작, 끝)` 한 쌍이다. 사각 선택은 **모양이 다르다**(줄마다 같은 열 구간).
복사·삭제·입력이 전부 그 모양을 알아야 하므로, 한 번에 다 하면 정렬 회귀가 난다.

그래서 **읽기(복사)까지만** 한다:

* 사각 범위를 **표현**하고(순수 타입 + 시험),
* 화면에 **그리고**,
* **복사**한다.

쓰기(사각 삭제·여러 줄 동시 입력)는 다음 배치. 절반만 하는 것이 아니라, 절반이 그 자체로
쓸모 있다 — 로그·표에서 한 열만 떠 가는 것이 사각 선택을 쓰는 가장 흔한 이유다.

---

## 결과 (v0.1.473)

### ⚠️ 계획의 중심이 헛다리였다 — E1은 이미 다 있었다

"터미널엔 있는데 편집기엔 없다"고 적고 순수 코어(`editrect.rs`)까지 만들었는데,
**`editbufboxsel.rs`가 Alt+드래그·표시열 매핑·여러 줄 동시 입력·삭제까지 전부 갖추고**
있었다. 내가 `rect_sel|col_sel|block_sel`로 찾았고 실제 이름은 `box_select`였다.
만든 것은 지웠다.

**다만 그 과정에서 진짜 버그를 하나 찾았다.** `selected_text`가 멀티범위에서 빈 범위를
걸러 내고 있었다 — 사각으로 고른 구간이 짧은 줄에서는 비는데, 그 줄을 빼면 **복사한
덩어리의 줄이 밀린다.** 3번 줄이 비어 있었다는 사실이 사라지고 4번 줄이 3번 자리로
올라온다. 들쭉날쭉한 로그에서 열을 떠 갈 때 바로 드러나는데, 붙여넣기 전에는 모른다.
고쳤고 `copying_a_ragged_box_keeps_every_row`가 지킨다.

교훈: **검색어를 한 벌만 쓰지 말 것.** 같은 개념의 이름이 프로젝트마다 다르다
(rect / column / block / box). 두 번째 시도에서 넓힌 검색어를 쓰니 나머지는 정확히 걸렸다.

### 한 것

| # | 결과 |
|---|---|
| E2 | `editfoldall.rs` — 전체 접기/펴기. **가장 바깥만** 접는다(안쪽까지 접으면 나중에 바깥을 펴도 안쪽이 접힌 채 남아 "안 펴진다"로 보인다). 들여쓰기 규칙이 메뉴 코드에 인라인돼 있던 것을 `EditBuf::fold_indent`로 뽑아 한 자리 접기와 같은 규칙을 쓰게 했다. |
| T2 | `nabi-render/pastepreview.rs` — 붙여넣기 확인창이 **첫 줄 60자만** 보여 주던 것을 실제 미리보기로. 이 창이 뜨는 이유는 붙여넣을 것이 위험할 수 있어서인데, 위험은 대개 두 번째 줄부터 온다(첫 줄은 그럴듯한 주석이고 그 아래 명령이 붙는 것이 고전적 수법). 숨은 문자는 `‹U+200B›`로 드러내고, 가운데를 접되 **몇 줄 접었는지 말한다**. |
| S1 | `connhist.rs` + `connhistui.rs` — 접속 이력. **파일로 남긴다** — 세션 한정으로 만들면 "어제 몇 시에 붙었더라"에 답할 수 없어 기능의 목적이 사라진다. 남기는 것은 호스트·사용자·시각·지속시간·끝난 이유까지고 **명령이나 화면은 남기지 않는다**(그건 세션 로그의 몫). 설정에서 끌 수 있다. |

### 설계에서 신경 쓴 것

- 이력 파일은 설정과 **따로** 둔다. 설정 `load`가 `unwrap_or_default`라 한 필드가 깨지면
  전체가 초기화되는데, 거기에 이력까지 얽히면 안 된다.
- 깨진 이력 파일은 빈 목록으로 시작한다 — 이력 때문에 프로그램이 안 뜨면 안 된다.
- 같은 호스트에 여러 창으로 붙어 있으면 **가장 최근에 연 것**부터 닫는다. pane 번호를
  파일에 남기면 다시 열 때 달라져 쓸모가 없다.
