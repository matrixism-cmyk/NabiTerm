# 배치 2026-08-25g — 11 phase, 다섯 분야

v0.1.466 직후. 계획 전 grep으로 **이미 있는 것을 대거 걸러냈다** — 미니맵·줄 북마크·세션
로깅·정규식 이스케이프·Find in Files 바꾸기·SFTP 필터·동기화 비교·마지막 접속 시각·
줄 이동/복제·포트포워딩 UI·OpenSSH config 가져오기는 전부 이미 있다. 이 저장소는 성숙해서
"흔한 기능"은 대부분 채워져 있고, 남은 것은 **더 깊은 자리**다.

## 벤치마킹 근거

* **자동 응답 트리거** — SecureCRT·MobaXterm의 대표 기능(expect-send). 우리에게 완전히 없다.
  `y/N` 확인, 반복되는 sudo 프롬프트, 배포 스크립트의 정형 질문에 손이 묶이지 않게 한다.
  AI가 띄우는 TUI 확인 프롬프트에도 그대로 쓰인다(우리 AI substrate 포지션과 맞물린다).
* **명령 소요 시간** — Warp 블록이 보여 주는 것. 우리는 OSC 133으로 시작·끝을 **이미 알면서**
  그 차이를 버리고 있다. "아까 그 빌드 얼마나 걸렸지"는 매일 나오는 질문이다.
* **정규식 강조** — 지금 키워드 강조는 글자 그대로만 맞춘다. 로그를 볼 때 필요한 것은
  `ERROR|FATAL`, `\b5\d\d\b` 같은 것이다.
* **두 문서 비교** — 디스크 원본과의 비교는 있는데(diff_disk) **열려 있는 두 문서**를 못 견준다.
* **위/아래 커서 추가** — Ctrl+Alt+↑/↓. 지난 배치에서 일치 기반 다중 커서를 냈으니 짝이 빈다.

## phase

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| T1 | 셸 | **자동 응답 트리거** — 출력 패턴에 정해 둔 답을 보낸다 | 위 | 높음(오작동=원격에 엉뚱한 입력) |
| T2 | 셸 | 트리거 **안전장치** — 세션당 on/off·횟수 상한·비밀번호 경고 | 위 | — |
| T3 | 셸 | **명령 소요 시간** 기록·표시(기록 창) | 위 | 낮음 |
| T4 | 셸 | **정규식 강조 규칙**(기존 키워드 강조에 옵션 추가) | 위 | 중(잘못된 정규식) |
| S1 | SSH | 세션 연결 시 **터널 자동 개통** | 매번 손으로 여는 수고 | 중 |
| S2 | SSH | 접속 후 **초기 명령 자동 실행** | 세션마다 늘 치는 첫 명령 | 중 |
| F1 | 파일관리자 | SFTP **이름 마스크 필터**(`*.log`) — 브라우저의 glob 재사용(DRY) | 부분일치만으로는 부족 | 낮음 |
| F2 | 파일관리자 | 미리보기 → **바로 편집으로 잇기** | 확인하고 고치는 흐름이 끊긴다 | 낮음 |
| M1 | 메뉴/IA | **도구 메뉴 정리** — 24항목이 한 줄로 늘어서 있다 | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | **두 문서 비교(diff)** | 위 | 중 |
| E2 | 에디터 | **위/아래 커서 추가** + 다중 커서 개수 표시 | 위 | 중 |

## 위험이 가장 큰 것 — T1

자동 응답은 **원격에 우리가 대신 글자를 보내는 일**이다. 잘못 맞으면 엉뚱한 곳에 `y`가
들어간다. 그래서 처음부터 다음을 못 박고 시작한다.

* 매칭은 **순수 함수**로 두고 시험을 두껍게 — 서버 없이 다 검증할 수 있어야 한다.
* **기본은 꺼짐.** 세션마다 켠다. 전역 자동 활성 없음.
* **횟수 상한**(같은 트리거가 연속으로 계속 발동하면 멈춘다) — 무한 응답 루프 방지.
* **비밀번호로 보이는 프롬프트에는 답하지 않는다.** 자동 응답으로 비밀번호를 보내는 것은
  볼트가 있는 이유를 스스로 무너뜨린다.
* 주입 텍스트는 **ASCII 전용**(기존 규율 — AI TUI 한글 깨짐).

## 결과

11 phase 중 **9개를 새로 구현했고, 2개는 이미 있었다.** 계획 단계 grep이 얕았던 탓이다.

| # | 결과 |
|---|---|
| T1·T2 | 자동 응답 트리거 — 기존 트리거 규칙에 `-> reply:` 액션을 더했다(목록을 둘로 나누지 않았다) |
| T3 | 명령 소요 시간 — 기록 창에 표시 |
| T4 | 정규식 강조 — `re:` 접두, 규칙 파서도 함께 고쳤다 |
| S1 | 세션 접속 시 터널 자동 개통 |
| S2 | **이미 있었다** — `SavedSession.on_connect`가 진작 구현돼 있었다 |
| F1 | **이미 있었다** — `browserfilter::name_matches`가 glob·포함·제외를 모두 지원하고 브라우저·SFTP가 공유한다 |
| F2 | 미리보기에서 편집·내려받기로 잇기 |
| M1 | 도구 메뉴 17개 평면 목록 → 5묶음(다시 부풀지 못하게 시험으로 잠금) |
| E1 | 열린 문서끼리 비교 |
| E2 | 위/아래 커서 추가(Ctrl+Alt+↑/↓) |

## 하다가 알게 된 것

**설정 스키마를 함부로 바꾸면 사용자 설정이 통째로 날아간다.** `load()`가
`.extract().unwrap_or_default()`라 **한 필드라도 파싱에 실패하면 전체가 기본값으로
초기화**된다. 명령 소요 시간을 `cmd_history` 튜플에 넣으려다 이걸 발견했다 — 4-튜플을
5-튜플로 바꾸면 옛 config.toml이 파싱에 실패하고 그 순간 모든 설정이 사라진다.
**새 필드를 더하는 것은 안전하다**(없으면 `#[serde(default)]`). 그래서 곁에 따로 뒀다.

**자동 응답은 알림 트리거와 보는 곳이 다르다.** 알림은 "새로 생긴 줄"을 보는데 프롬프트는
줄바꿈 없이 커서 앞에 머문다 — 새 줄로는 영영 안 잡힌다. 그래서 자동 응답만 화면 아래
몇 줄을 본다. 컴파일러가 `Action::Reply`를 알림 루프에서 다루라고 강제해 준 덕에 이 구분을
놓치지 않았다.

**정규식이 들어오자 옛 규칙 파서가 드러났다.** 강조 규칙은 **첫** `=`에서 잘랐는데,
`re:a=b` 같은 패턴이 흔해 `b`를 색으로 오해한다. 뒤에서 찾고 **진짜 색일 때만** 색으로
보도록 고쳤다(옛 규칙은 그대로 동작한다).

**설정 검색 드리프트 게이트가 첫 실전에서 일을 했다.** 자동 응답 스위치를 넣자마자
`settings.autoreply`가 검색 표에 없다고 정확히 짚었다. 지난 배치에 만든 장치가 만든 사람을
먼저 잡은 셈이다.

**S1은 세션 파일 대신 설정에 뒀다.** `SavedSession` 생성 지점이 서른 곳이 넘고 대부분
필드를 하나하나 적는다. 배치 끝에서 서른 곳을 건드릴 값어치가 없었고, `last_connected`가
이미 세션 이름을 키로 설정에 사는 선례가 있다.
