# 업그레이드 배치 V — 2026-08-26 · **LSP를 러스트 밖으로**

v0.1.481 이후.

## 왜 이것인가

세 기둥 중 편집기는 "VS Code·EmEditor 수준을 지향한다"고 적어 두었다. 그런데 코드 지원의
핵심인 LSP가 **러스트 하나에만** 붙어 있다:

```rust
    // editorlsp.rs
    && doc.lang_ext() == "rs"                        // 이 문서만 LSP 대상
    self.lsp.client = LspClient::start("rust-analyzer", &root);   // 서버도 하나
    // editormenu.rs · editorctx.rs
    if doc.lang_ext() == "rs" { /* 코드 메뉴 */ }     // 메뉴도 러스트만
```

파이썬·타입스크립트·Go 파일을 열면 정의로 가기도, 이름 바꾸기도, 진단도 없다. **VS Code를
지향한다면서 언어 하나만 되는 것**은 가장 큰 구멍이다.

다행히 **코어는 이미 일반화돼 있다** — `LspClient::start(server, root)`는 서버 이름을 받는다.
막고 있는 것은 앱 쪽 하드코딩 세 곳뿐이다.

## 확인 결과

| 후보 | 판정 |
|---|---|
| LSP 서버 여럿 | **없다.** `LspState.client`가 하나, 이름은 `"rust-analyzer"` 고정 |
| 언어→서버 매핑 | **없다** |
| 서버 명령 설정 | **없다**(사용자 환경마다 경로가 다른데) |
| 서버 없을 때 안내 | 조용히 비활성 — 왜 안 되는지 알 길이 없다 |
| 트리시터 강조 | **이미 여러 언어**(`ropets`: rs·py·js·json·toml) — 강조는 되는데 LSP만 러스트 |
| 프로젝트 루트 찾기 | `cargo_root`(Cargo.toml)만 |

트리시터가 이미 다섯 언어를 강조한다는 점이 이 구멍을 더 뚜렷하게 한다 — **색은 입혀
주면서 정의로는 못 간다.**

## 계획 (12 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| L1 | 에디터 | **언어→서버·루트 표식** 표(순수, 시험) | 나머지가 전부 여기 기댄다 | 낮음 |
| L2 | 에디터 | 프로젝트 **루트 찾기 일반화**(Cargo.toml 외) | 루트가 틀리면 서버가 헛돈다 | 중 |
| L3 | 에디터 | **여러 서버 동시 관리**(문서마다 다른 서버) | 지금은 하나뿐 | **높음** |
| L4 | 에디터 | 메뉴·컨텍스트 조건을 표 기반으로 | 하드코딩 세 곳이 따로 논다 | 중 |
| L5 | 에디터 | 서버가 없으면 **왜 없는지** 알린다 | 조용히 비활성 = 고장으로 읽힌다 | 낮음 |
| L6 | 에디터 | 서버 명령을 **설정으로** 바꾸기 | 환경마다 경로가 다르다 | 중 |
| M1 | 메뉴/IA | 설정에 LSP 묶음 + 상태 표시 | 흩어 두면 못 찾는다 | 낮음 |
| M2 | 메뉴/IA | 색인·i18n 배선 | 검사기가 잡는 곳 | 낮음 |
| T1 | 셸 | 도구 설치 안내와 잇기(환경 관리자) | 없는 서버를 깔 길을 준다 | 낮음 |
| F1 | SFTP | 원격 파일은 LSP 대상이 아님을 **명시** | 지금은 조용히 빠진다 | 낮음 |
| V1 | 검증 | 러스트 경로가 **그대로 동작**하는지 | 되던 것이 깨지면 회귀다 | 중 |
| V2 | 검증 | 서버가 없는 언어에서 조용히 지나가는지 | 없는 것이 기본 상태다 | 낮음 |

## 가장 조심할 것

* **되던 것을 깨지 않는다.** 러스트 경로가 지금과 똑같이 동작해야 한다. 이 배치의 성패는
  새 언어가 되느냐가 아니라 **러스트가 그대로냐**에 먼저 달려 있다.
* 서버는 **없는 것이 기본**이다(대부분의 사용자는 pyright를 안 깔았다). 없을 때 조용하고
  가볍게 지나가야 하며, **한 번 실패한 서버를 매 프레임 다시 띄우려 하면 안 된다.**
* 문서마다 서버가 다르므로 **동기화 상태도 서버별로** 갈라야 한다. 지금은 경로→해시 맵
  하나뿐이라, 그대로 두면 다른 서버에 남의 문서를 알리게 된다.

## 결과 (v0.1.482 예정)

### L1 — 언어·서버·루트 표 (`lspservers.rs`)

다섯 언어를 안다: Rust · Python · TypeScript/JavaScript · Go · C/C++. 항목마다 **루트
표식**이 다르다(`Cargo.toml`·`pyproject.toml`·`package.json`·`go.mod`·`compile_commands.json`).
루트를 잘못 주면 서버가 남의 파일을 훑거나 아무것도 못 찾는다.

**시험이 결함을 잡았다.** `Path::new("a.rs").parent()`는 `None`이 아니라 **빈 경로**를
돌려준다. 그대로 두면 언어 서버에 빈 루트를 넘기게 되고, 그때 서버가 무엇을 훑을지는
서버 마음이다.

### L3 — 서버 여럿, 그리고 **요청 번호가 겹치는 문제**

서버가 여럿이면 곧바로 부딪히는 것이 있다: **두 서버가 각자 1번부터 센다.** 대기 중인
응답을 번호만으로 꺼내면 파이썬 서버의 답을 러스트 요청의 답으로 착각할 수 있다.

그래서 대기 중인 요청마다 **어느 서버의 것인지**(키)를 함께 들고 다니게 했다. 일곱 개
대기 칸이 전부 바뀌었지만, 이 한 가지를 안 하면 나머지가 다 위태롭다.

빌림 문제도 하나 나왔다 — 서버 손잡이를 오래 들고 있으면 같은 구조체의 동기화 기록을
못 고친다. 매번 키로 다시 찾아 **짧게 쓰도록** 바꿨다.

### L4·L5 — 하드코딩 세 곳과, 조용한 비활성

`== "rs"`가 세 곳(동기화 판정·메뉴·컨텍스트)에 박혀 있었다. 전부 표를 보게 했다.

서버가 없을 때는 **왜 없는지** 한 번 알린다(`pyright-langserver`를 깔면 된다는 것은
알려 줄 수 있는 정보다). 매 프레임 같은 말을 띄우면 알림이 아니라 방해이므로, 못 띄운
서버를 기억해 두고 **처음 한 번만** 말한다. 한 번 실패한 서버는 세션 내 다시 띄우지
않는다 — 매 프레임 프로세스를 띄우려 드는 것이 이 자리의 가장 흔한 사고다.

### 파일이 커져 세 번 나눴다

`editorlsp.rs`가 소프트 한도를 넘어 요청 부분을 `editorlspreq.rs`로 옮겼다. 한 번에
안 맞아 세 번에 걸쳐 옮겼는데, 그때마다 게이트가 정확히 얼마나 넘었는지 알려 줬다.

### 되던 것은 그대로다

이 배치의 성패는 새 언어가 아니라 **러스트가 그대로냐**에 먼저 달려 있었다. 표의 첫
항목이 예전 값과 정확히 같은지 시험이 지킨다(`rust-analyzer`·`Cargo.toml`·인자 없음).

## 검증

```text
clippy 0 · lines 신규 초과 0 · 한 줄 길이 초과 0 · prerelease 0
```

---

## 남은 phase 마무리 (2026-08-27, 다른 세션이 이어받음)

이 배치를 시작한 세션이 L1~L5까지 하고 멈췄다. 확인해 보니 **다른 Claude 세션이
하나도 돌고 있지 않아서**(`ListAgents`) 아무도 이어받지 않을 상태였고, 그대로 두면
v0.1.482가 반쪽으로 나간다. 그래서 남은 것을 마저 했다.

| # | 결과 |
|---|---|
| M1 | `settingslsp.rs` — nabiPad 설정 창에 **언어 서버 묶음**. `SERVERS` 표를 그대로 읽어 각 서버가 PATH에 있는지 ✓ / — 로 보여 준다. 표가 유일한 출처라 서버를 더해도 화면을 고칠 필요가 없다. |
| M2 | i18n 키 넷(설정 제목·설명·없음·설치 안내). |
| T1 | 환경 관리자에 **언어 서버 셋**(rust-analyzer · pyright · typescript-language-server). "PATH에 없다"고 알려 주기만 하고 깔 길을 주지 않으면 절반만 한 것이다. |
| — | 그 과정에서 `NPM:` 통로가 없다는 것을 발견해 만들었다(`npm_install_script`). Node가 없으면 **깔지 않고 알려 준다** — 여기서 Node까지 받아 오면 `aicli::install_npm_cli`와 같은 일을 두 벌로 하게 된다. |

**설정 화면을 어디에 둘지 한 번 헛짚었다.** 메인 설정에 편집기 페이지가 있는 줄 알고
`NabiApp` 메서드로 썼는데, 실제로는 nabiPad **자체 설정 창**에만 있었다(그 창은 앱
상태를 모른다). 자유 함수로 바꿔 두 곳 모두에서 쓸 수 있게 했다.
