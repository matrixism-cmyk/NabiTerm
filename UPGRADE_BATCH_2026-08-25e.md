# 배치 2026-08-25e — 환경 관리자(사용자 요청 우선) + 5분야

사용자 요청(2026-08-25): ① 도움말의 "AI 제어"를 설정으로 옮길지 의견 ② WSL 배포판·PowerShell
업그레이드·gh/sshpass 같은 도구를 **클릭만으로** 설치.

결론: ①은 **성격으로 가른다** — 설명은 도움말, 설치/제거는 설정. ②와 같은 일이므로 하나의
새 설정 페이지 **"환경/도구"**로 합친다(메뉴 비대화 방지 = 매 배치 IA phase).

## 현장 확인으로 뒤집힌 가정 두 가지

* `wsl --list --online`은 **UTF-16LE**로 쓴다. 바이트를 그대로 UTF-8로 읽으면 글자 사이에
  NUL이 낀 쓰레기가 된다(직접 봤다). 파서는 UTF-16LE로 디코딩해야 한다.
* **이 PC에 winget이 없다**(Windows Server 2022). winget만 쓰는 설계는 서버 사용자에게
  통째로 실패한다 → 도구마다 winget 경로와 **직접 내려받기 경로를 둘 다** 둔다.

## phase

| # | 분야 | 항목 | 근거 |
|---|---|---|---|
| A1 | 메뉴/IA | 설정에 **환경/도구** 페이지 신설, 도움말의 AI CLI 관리자를 이관 | 설치는 "읽는 곳"이 아니라 "바꾸는 곳"에 있어야 한다 |
| A2 | 셸 | **WSL 배포판 카탈로그** — 설치 가능한 목록을 표로 보이고 클릭 설치 | CLI에 익숙하지 않으면 무엇을 고를 수 있는지조차 알 길이 없다 |
| A3 | 셸 | **PowerShell 7** 설치 + 나비텀 기본 셸로 전환 | 윈도우 전역 기본을 바꾸는 건 되돌리기 어렵다 — 우리 앱 기본까지가 안전선 |
| A4 | 셸 | **개발 도구**(gh·ripgrep·fzf·jq…) 원클릭 설치/제거 | 반복되는 수작업 |
| A5 | 공통 | 도구마다 winget/직접내려받기 **이중 통로**, winget 없으면 자동 우회 | 위 확인 사실 |
| A6 | 공통 | **단계 기반 진행률**(스크립트가 `@@STEP i/n`을 뱉고 UI가 그대로 읽음) | 사용자 지적: "한세월 기다리면 창을 닫아버린다" |
| A7 | 공통 | sshpass처럼 **윈도우에 없는 것은 없다고 정직히 표시**하고 대안 안내 | 되는 척이 제일 나쁘다 |
| S1 | SSH | 에이전트 포워딩 시험을 **진짜 판별식**(원격 `SSH_AUTH_SOCK`)으로 교정 + 루프백 거부 | 이전 시험은 자기 자신에 붙어 아무것도 증명 못 했다 |
| S2 | SSH | Windows sshd는 포워딩 미구현임을 문서화(이 PC로는 검증 불가) | 확인 사실 기록 |
| M1 | 메뉴/IA | 팔레트·메뉴에 환경 관리자 진입점 일원화 | 드리프트 금지 |
| E1 | 에디터 | (다음 회차로) — 이번 배치는 환경 관리자에 집중 | 사용자 요청 우선 |

## 실제로 돌려 보고서야 나온 결함 (전부 이 PC에서 확인)

코드만 봐서는 하나도 안 보였다. "모양이 맞는가"를 보는 순수 시험은 전부 통과했고,
실제로 설치해 보고서야 드러났다.

| 결함 | 무엇이 잘못됐나 | 어떻게 고쳤나 |
|---|---|---|
| 내려받기 404 | `releases/latest/download/<고정이름>.msi`는 PowerShell·gh 둘 다 404. 자산 이름에 판 번호가 들어간다 | GitHub API로 최신 자산을 그때그때 찾는다 |
| winget 0x80073CF3 | VCLibs·UI.Xaml만으로는 부족. 요즘 winget은 WindowsAppRuntime까지 필요하고 목록이 판마다 바뀐다 | MS가 릴리스에 올리는 `DesktopAppInstaller_Dependencies.zip`을 그대로 쓴다 |
| winget 무면허 | `Add-AppxPackage`만 하면 **설치는 성공하고 PATH에도 오르는데 실행하면 죽는다**("No applicable app licenses found") — 되는 척만 한다 | 관리자면 라이선스와 함께 프로비저닝하고, **끝에 실제로 실행해 본다** |
| PATH 미갱신 | gh를 깔았는데 안 보인다. 설치 프로그램이 고친 PATH는 이미 뜬 프로세스에 오지 않는다 | 설치 후 레지스트리에서 다시 읽어 우리 프로세스에 얹는다(`envpath`) |
| 실패 이유 소실 | stderr를 안 받아 "설치 명령 실패" 여섯 글자만 남았다. 진짜 이유가 통째로 사라졌다 | stderr를 별도 스레드로 받아(파이프 막힘 방지) 이유를 보존 |

## 사용자 보고 버그 — 로컬 터미널 목록

**증상**: 안 깐 WSL이 목록에 보이고, 깔린 셸이 목록에 없다.

**원인**: `wsl.exe`는 WSL을 한 번도 깔지 않은 윈도우에도 `System32`에 **항상 있다**. 그래서
"PATH에 wsl.exe가 있는가"라는 물음의 답은 언제나 예였다. 물음이 틀렸다.

**고침**: 물음을 "쓸 수 있는 배포판이 하나라도 있는가"로 바꿨다(`shelldetect::wsl_entries`).
반대 방향으로는 Nushell·Cygwin·MSYS2·PowerShell 7 미리보기·32비트 Windows PowerShell을
`ShellKind::Custom`으로 더했다(열거형을 건드리지 않는다). 목록 캐시가 `OnceLock`이라 다시
켜기 전엔 갱신되지 않던 것도 `RwLock`으로 바꿔, 환경 관리자에서 깔면 그 자리에서 나타난다.

**이 PC 실측**: 설치 전 4종(WSL 없음) → PowerShell 7 설치 후 5종(재시작 없이 즉시 반영).
