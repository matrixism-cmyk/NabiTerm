# 업그레이드 배치 계획 — 2026-08-25 (2회차, v0.1.463~)

목표: **상용 제품 이상.** 전수 grep 감사 후 진짜 빈 곳만 골랐다.

## 하지 않는 것 (이미 있음 — 감사로 확인)

| 후보 | 실제 위치 |
|---|---|
| bracketed paste · OSC 133 프롬프트 표식 · 프롬프트 점프 | `nabi-render/input.rs` · `nabi-osc` · `nabi-vt/prompts.rs` |
| 분할 pane 최대화 토글 | `viewportcmd.rs toggle_pane_zoom` (Ctrl+Shift+Z) |
| 탭 이름 바꾸기 · 탭 복제 | `tabmenu.rs` · `tabops.rs` |
| SFTP 원격 편집 · 원격 파일 검색 | `editsftp.rs` · `sftptoolbar.rs` |
| SFTP 전송 속도 제한 | `nabi-sftp/fs.rs throttle_delay` + 설정 |
| 폴더 크기 계산 | `BrowserAct::calc_size` |
| nabiPad 줄 이동·북마크·열 선택·폴딩·미니맵 | `editorfind.rs` · `editbufboxsel.rs` · `editbuffold.rs` · `editorminimap.rs` |
| 파일 비교(diff) · 줄 정렬·중복 제거 | `difflines.rs` · `editmenugroups.rs` |
| 빠른 연결의 `ssh user@host -p` 파싱 | `qcparse.rs` |
| 스크롤백 크기 설정 · 버퍼 비우기 | `schema.rs scrollback` · `PaletteAction::ClearBuffer` |

## 이번 배치 (12 phase, 5분야)

### ⓿ 사용자 보고 (최우선 — 완료)
| # | 항목 | 결과 |
|---|---|---|
| U1 | **휠로 이전 내용을 못 보던 문제** | ✅ DEC 1007을 주 화면에서도 따르던 우리 버그. xterm 규격은 대체 화면 한정 |
| U2 | **`pane-modes` 진단 verb** | ✅ 보이지 않는 터미널 모드를 확인 — 다음엔 추측하지 않는다 |

### ① 셸/터미널
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| T1 | **시작 시간 계측 로그** | 감사 결과 계측이 **전무**하다. 느려져도 언제부터인지 알 수 없다. 상용 제품은 자기 성능을 안다 | 하 / 낮음 |
| T2 | **pane별 스크롤백 상한** | 지금은 전역 하나. AI TUI는 재그리기로 스크롤백을 빨리 먹는데, 그 pane만 크게 잡을 수 없다 | 중 / 낮음 |

### ② SSH
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| S1 | **에이전트 포워딩**(`auth-agent-req@openssh.com`) | 감사 결과 로컬 인증은 있고 포워딩만 없다. 원격에서 다시 git/ssh를 쓰는 흐름에 필수 | 상 / 중간 |

### ③ 파일관리자·SFTP
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| F1 | **파일 속성 대화상자** | 크기·시각·권한·해시를 한자리에. 탐색기·FileZilla 모두 기본 | 중 / 낮음 |
| F2 | **가져오기 한 화면**(설치 자동 탐지) | 임포터 7종이 메뉴에 흩어져 있다. 전환 장벽 제거가 목적인데 찾기 어려우면 의미가 없다 | 중 / 낮음 |

### ④ 메뉴·IA (매 배치 1+ 필수)
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| M1 | **팔레트에 단축키 표시** | 감사 결과 없음. 팔레트는 단축키를 배우는 자리이기도 하다 | 하 / 낮음 |
| M2 | **팔레트 최근 사용 우선** | 지금은 정적 순서. 자주 쓰는 것이 위로 와야 한다 | 중 / 낮음 |
| M3 | **설정 검색(생성기 방식)** | 지난 회차 보류. 설정 키 107개를 **소스에서 색인 생성**하고 xtask가 어긋남을 잡는다 | 상 / 중간 |
| M4 | **업데이트 후 '새로워진 점'** | 지금은 업데이트 *전* 알림만. 무엇이 바뀌었는지 설치 후에는 볼 수 없다 | 중 / 낮음 |

### ⑤ 에디터(nabiPad)
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| E1 | **커서 추가 제스처**(Ctrl+D 다음 항목 선택 / Alt+클릭) | 감사: 멀티커서 자료구조는 있는데 **추가하는 방법이 없다**. VS Code의 대표 동작 | 중 / 중간 |
| E2 | **접근성 1단계** | eframe `accesskit`은 **이미 켜져 있다**(감사로 정정). 앱 쪽 작업이 없을 뿐 — 위젯 라벨·포커스 순서·고대비 | 상 / 중간 |

## 순서
U1·U2 ✅ → M1 → T1 → F1 → M4 → E1 → F2 → T2 → M2 → M3 → E2 → S1 → 게이트 → 11:00 창 배포
