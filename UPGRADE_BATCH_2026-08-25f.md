# 배치 2026-08-25f — 11 phase, 다섯 분야

v0.1.465 직후. 계획 전 grep으로 **이미 있는 것을 걸러냈다**: 붙여넣기 확인·copy-on-select·
박스(컬럼) 선택·SFTP 동기화 미리보기·터미널 출력 저장은 이미 구현돼 있어 후보에서 뺐다.

## 벤치마킹 근거

* **연결 실패 진단** — PuTTY·MobaXterm·Termius는 실패를 원문 그대로 던지지 않는다. 우리는
  지금 `[ssh 오류: <russh 원문>]` 한 줄이 전부다. 영어 원문 한 줄은 국내 사용자에게
  아무것도 알려 주지 않는다(무설정 한글이 우리 차별화인데 실패 순간에 무너진다).
* **명령 기록** — Warp의 블록, Fig/Atuin의 명령 히스토리. 우리는 OSC 133 프롬프트 표식
  (`nabi-vt/prompts.rs`)으로 명령 경계와 종료 코드를 **이미 알고 있는데** 그것을 목록으로
  보여 주지 않는다. AI와 바이브코딩하는 흐름에서 "아까 그 명령"을 되찾는 값이 크다.
* **설정 검색** — VS Code 설정의 핵심 UX. 우리 설정은 6페이지 35항목이 넘어 눈으로 찾는다.
* **팔레트 MRU** — 명령이 86개다. 자주 쓰는 것이 위로 오지 않으면 팔레트의 뜻이 준다.
* **다중 커서(Ctrl+D)** — VS Code·Sublime의 대표 기능. `editsel::Selection`이 이미
  "정렬·비겹침 범위 집합"이라 토대가 있는데 **일치로 커서를 늘리는 길만 없다.**

## phase

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| S1 | SSH | 연결 실패를 **원인별로 분류**하고 사람 말 + 해결 실마리로 바꾼다(순수 함수) | 위 | 낮음(순수) |
| S2 | SSH | 진단을 pane 출력에 한글로 띄우고, 인증 실패면 **무엇을 시도했는지**까지 말한다 | 위 | 낮음 |
| T1 | 셸/터미널 | **명령 기록** — pane이 실행한 명령을 표식에서 모아 목록으로 | 위 | 중(표식 의존) |
| T2 | 셸/터미널 | 기록에 **종료 코드·성공 실패**를 함께, 실패만 보기 | 위 | 낮음 |
| F1 | 파일관리자 | SFTP **원격 파일 미리보기**(앞부분만 바운디드로 읽어 텍스트 확인) | 내려받지 않고 확인 | 중(원격 I/O) |
| M1 | 메뉴/IA | **설정 검색** — 낱말로 항목을 찾아 그 페이지로 보낸다 | 위 | 낮음 |
| M2 | 메뉴/IA | **팔레트 최근 사용 우선** | 위 | 낮음 |
| E1 | 에디터 | **Ctrl+D 다음 일치 추가**(다중 커서) | 위 | 중(선택 모델) |
| E2 | 에디터 | **모든 일치 선택** | 위 | 중 |
| E3 | 에디터 | 다중 커서 상태에서 되돌리기·타자 정합 확인(시험으로 못 박음) | 회귀 방지 | 낮음 |
| M3 | 메뉴/IA | 줄 수 경고 감축(27건 → 20건 이하) | 규율 | 낮음 |

## 이어서 할 일(다음 회차)

* 에이전트 포워딩 실검증 — **리눅스 서버가 있어야 한다**(Windows sshd는 미구현).
* AI 산문 한글화 + 번역 캐시 축적(사용자 승인, 로컬 전용).

## 결과

11 phase 전부 구현했다. 아래는 **하다가 알게 된 것**들 — 계획 단계에서는 몰랐던 것이다.

| 발견 | 내용 |
|---|---|
| `cmdhist`가 이미 있었다 | 명령·작업폴더·종료코드·시각을 진작부터 모으고 있었는데 볼 방법이 팔레트 몇 줄뿐이었다. T1은 "처음부터 만들기"가 아니라 "그 위에 창을 얹기"가 됐다. grep 먼저 하는 규율이 또 한 번 값을 했다. |
| `Selection`이 멀티커서를 기다리고 있었다 | 자료구조는 이미 "정렬·비겹침 범위 집합"이었고 주석에 "멀티커서 UI가 붙기 전까지"라고 적혀 있었다. 우리가 낸 것은 **범위를 늘리는 길** 하나였다. |
| 겹치는 일치를 세 번 셌다 | `"aaaa"`에서 `"aa"`를 셋이라고 답했다. push가 겹치면 병합하는데 센 횟수를 따로 세고 있었다. 편집기의 일치는 겹치지 않는다 — 찾기도 `at + n`으로 건너뛰게 고쳤다. |
| 시험이 사용자와 다른 길을 밟고 있었다 | 다중 커서 편집 시험이 `insert`(주 범위 하나)를 불렀는데 실제 타자 경로는 `insert_multi`다. 그대로 뒀으면 통과하면서 아무것도 지키지 못했을 것이다. |
| 검사기가 자기 패턴에 걸렸다 | 설정 훑기 코드에 적힌 `tr(lang, "settings.` 문자열을 i18n 키 검사기가 **진짜 키로 오해**해 게이트가 실패했다. 찾을 낱말을 조각으로 나눠 해결. |
| catalog3이 하드 한도를 넘었다 | 412줄(한도 400) — 이번 배치 게이트 실패. catalog6으로 나눴고, 겸사겸사 `tr`과 무결성 시험에 **복사돼 있던 체인을 하나로 합쳤다**(카탈로그를 더할 때 한쪽만 고치면 조용히 어긋나던 자리). |

### 검증

* 연결 실패 진단: 실제로 거부·이름풀이실패를 일으켜 확인. **원문이 한국어로 왔다**
  (`대상 컴퓨터에서 연결을 거부했으므로…`) — 영문만 맞췄으면 못 잡았고, 오류 **번호**를
  같이 본 덕에 잡혔다.
* SFTP 미리보기: 실 OpenSSH 서버로 4가지(상한 초과·미만·**딱 상한**·빈 파일) 확인.
  딱 상한인 경우에 "더 있음"을 띄우면 거짓말이 되는데 그 경계를 시험이 지킨다.
  기존 실서버 17건도 전부 통과. 검증용 키·authorized_keys는 **원상복구했다**.
* 설정 검색 표: 일부러 설정 행을 하나 더해 보고 드리프트 게이트가 그 키를 짚어내는 것까지 확인.

### 못 한 것

* M3 목표는 경고 20건 이하였는데 **25건에서 멈췄다.** 하드 실패는 없앴고 시작(27건)보다
  줄였지만, 나머지는 이번 배치에서 건드리지 않은 파일들의 오래된 빚이다. 손대면 회귀
  위험만 늘어 다음 배치로 미룬다.
