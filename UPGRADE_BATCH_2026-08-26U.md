# 업그레이드 배치 U — 2026-08-26 · **바깥으로 나가지 않기(폐쇄망)**

v0.1.480 이후.

## 우리가 내건 차별화인데 정작 그 모드가 없다

메모리의 정체성 문서에 이렇게 적혀 있다:

> 한국 차별화 = 무료 · 무설정 한글 · **폐쇄망**

그런데 폐쇄망 사용자를 위한 것이 코드에 **하나도 없다**. `오프라인`·`폐쇄망`·`airgap`으로
훑으면 나오는 것은 주석 두 줄뿐이고(팁 번역·접속 시간 제한), 스위치는 없다.

그러는 동안 프로그램은 **묻지 않고 밖으로 나간다**:

| 어디로 | 무엇 때문에 | 언제 |
|---|---|---|
| `api.ipify.org` | 상태바의 공인 IP | **켤 때마다** |
| `api.github.com` | 새 판 확인 | 주기적 |
| `github.com` 등 | 글꼴 내려받기 | 사용자가 누를 때 |
| npm·pip 등 | AI CLI 설치·갱신 | 사용자가 누를 때 / 자동 갱신 |

앞의 둘이 문제다. **사용자가 시키지 않았는데** 나간다.

* 폐쇄망(정부·금융·공장)에서는 이런 호출이 **보안 경보**를 띄운다. 프록시 로그에 남고,
  실패하면 `?`와 시간 초과만 남긴다.
* 폐쇄망이 아니어도, 공인 IP 조회는 **내 IP를 제삼자에게 알려 주는 일**이다. 켤 때마다,
  묻지도 않고.

## 확인 결과

| 후보 | 판정 |
|---|---|
| 오프라인/폐쇄망 스위치 | **없다** |
| 나가는 곳 목록(사용자가 볼 수 있는) | **없다** |
| 공인 IP 조회 끄기 | **없다**(늘 조회한다) |
| 업데이트 확인 끄기 | 자동 설치 설정은 있으나 **확인 자체를 막는 것은 없다** |
| 접속 시간 제한 | **이미 있다**(배치 R) — 건너뜀 |
| 비밀 가리기 | **이미 있다**(배치 T) — 건너뜀 |

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| N1 | 네트워크 | **나가는 곳을 한 곳에**(목록+사유, SSOT) | 흩어져 있으면 아무도 전체를 모른다 | 낮음 |
| N2 | 네트워크 | **오프라인 모드** 스위치 | 폐쇄망은 우리가 내건 차별화다 | 중 |
| N3 | 네트워크 | 공인 IP 조회를 **끌 수 있게** | 묻지 않고 IP를 제삼자에게 보낸다 | 중 |
| N4 | 네트워크 | 업데이트 **확인**도 오프라인이면 건너뛴다 | 프록시 로그에 남는다 | 중 |
| N5 | 네트워크 | 상태바에 **오프라인 표시** | 켠 줄 모르면 "왜 안 되지"가 된다 | 낮음 |
| H1 | 도움말 | **"이 프로그램이 연결하는 곳"** 페이지 | 감사·보안 검토에 필요한 답 | 낮음 |
| M1 | 메뉴/IA | 설정에 네트워크 묶음 | 흩어 두면 못 찾는다 | 낮음 |
| M2 | 메뉴/IA | 설정 색인·i18n 배선 | 검사기가 잡는 곳 | 낮음 |
| E1 | 에디터 | **문서에서 비밀 찾기**(배치 T에서 미룬 것) | 규칙이 시험을 거쳤다 | 중 |
| F1 | SFTP | 올리기 전 비밀 **개수만** 알림 | 되돌리기 어려운 일이다 | 중 |
| V1 | 검증 | 오프라인에서 **정말 안 나가는지** | 스위치가 스위치 노릇을 하는지 | 중 |

## 가장 조심할 것

* **기본값은 바꾸지 않는다.** 오프라인 모드는 꺼짐이 기본이다. 다만 공인 IP 조회처럼
  "묻지 않고 나가는" 것은 **끌 수 있게** 해 두는 것이 최소한의 도리다.
* 오프라인이라고 **조용히 실패하면 안 된다.** 안 하는 것과 못 하는 것은 다르다 —
  안 하는 것이면 그렇다고 말한다.
* E1·F1(비밀 경고)은 배치 T에서 "오탐이 귀찮게 한다"며 미룬 것이다. 그래서 **막지 않는다** —
  저장·업로드를 가로막는 모달이 아니라, 눌러서 확인하는 **찾기**와 **개수 알림**으로 한다.

## 결과 (v0.1.481 예정)

### N1 — 나가는 곳을 코드에 (`egress.rs`)

"이 프로그램이 어디에 연결합니까?"는 보안 검토에서 반드시 나오는 질문이다. 그 답이
문서에만 있으면 **코드가 바뀔 때 조용히 틀린 답**이 된다. 그래서 목록을 코드에 두고
도움말이 그것을 읽는다. 새 호출을 넣는 사람은 표에 한 줄을 더해야 한다.

항목마다 `unattended`(시키지 않아도 나가는가)를 달았다. 이 한 칸이 오프라인 모드가
무엇을 막고 무엇을 안 막는지를 정한다.

### N2~N4 — 오프라인 모드

막는 것은 **사용자가 시키지 않은 호출**뿐이다:

- 켤 때마다 하던 공인 IP 조회(`api.ipify.org`)
- 주기적 새 판 확인(`api.github.com`)
- AI CLI 자동 갱신

막지 않는 것: 글꼴 내려받기, AI CLI 설치, 도움말에서 **손으로 누른** 업데이트 확인.
**눌렀는데 아무 일도 없으면 그건 보호가 아니라 고장이다.**

공인 IP는 오프라인과 별개로도 끌 수 있게 했다. 폐쇄망이 아니어도 그것은 **내 IP를
제삼자에게 알려 주는 일**이고, 지금까지 묻지도 않고 켤 때마다 했다.

끈 자리에는 `?`(실패)가 아니라 `—`(안 함)를 둔다. **안 하는 것과 못 하는 것은 다르다.**

### N5·H1 — 보이게

상태바에 `✈ 오프라인`이 뜬다. 켠 줄 모르면 "왜 새 판이 안 뜨지"가 된다. 도움말에
**네트워크** 페이지를 더해 어디로·무엇 때문에·언제 나가는지를 표로 보여 준다.

### E1·F1 — 배치 T에서 미룬 것을, 미룬 까닭을 지키며

배치 T에서 "오탐이 곧바로 사용자를 귀찮게 한다"며 미뤘다. 그 판단을 지키는 설계로 넣었다:

- 편집기: 저장을 막는 모달이 **아니라** 눌러서 찾는 명령(`이 문서에서 비밀 찾기`).
  걸린 줄 번호를 알려 준다.
- 업로드: 막지 않고 **개수만** 한 줄로 알린다. 걸린 것이 없으면 **아무 말도 하지 않는다.**

규칙은 새로 만들지 않았다 — 배치 T의 가리기 규칙이 **바꾼 줄**이 곧 비밀이 든 줄이다.
규칙이 하나면 가리는 것과 찾는 것이 어긋날 수 없고, 그 사실을 시험이 지킨다.

## 검증

```text
clippy 0 · lines 신규 초과 0 · 한 줄 길이 초과 0 · prerelease 0
```
