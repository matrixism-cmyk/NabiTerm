# 업그레이드 배치 X — 2026-08-27 · **찾을 수 있게 만든다**

v0.1.481 이후(배포 대기 커밋 둘 위에 쌓는다). 배치 V(LSP)는 다른 세션 진행 중 —
`editorlsp*.rs` · `lspclient.rs` · `lspservers.rs` · `oncmdready.rs` 는 건드리지 않는다.

## 왜 이것인가

사용자 지시(2026-08-27): **지속 배포로 사용자층을 넓히고, 언젠가 AI 회사에 매각할 수
있을 만큼 키우고 싶다.** 그래서 이번 배치는 기능이 아니라 **유통**을 다룬다.

측정한 사실:

| 지표 | 값 |
|---|---|
| 총 다운로드 | **78회** (릴리스 36개 중 11개는 0회) |
| 별 · 포크 · 워처 · 이슈 | 0 · 0 · 0 · 0 |
| 코드 | 26 크레이트 · 1601 시험 · clippy 경고 0 |

**병목은 품질이 아니다.** 품질은 1인 프로젝트로는 이례적으로 높은데 아무도 이것이
있는 줄 모른다. 기능을 하나 더 만드는 것보다 **찾을 수 있게 만드는 것**이 지금 크다.

### 매각을 목표로 둘 때 실제로 값이 매겨지는 것

Windsurf 사례에서 값이 매겨진 것은 코드가 아니라 **팀·사용자·매출**이었다. 우리는
Apache-2.0로 소스를 공개했으므로 **코드 자체의 독점 가치는 0에 가깝다** — 값은
사용자·브랜드·속도에서 나와야 한다.

그리고 AI 회사가 탐낼 자산은 터미널이 아니라 **제어 평면**이다: AI 에이전트가 윈도우에서
실제 터미널·SSH·SFTP를 조작하는 통로. 그런데 지금 그것은 **도움말 안쪽에 묻혀 있다.**
설치한 사람도 있는 줄 모르고, 저장소를 지나가는 사람은 더더욱 모른다.

### 추적하지 않으면서 세는 법

우리는 사용 기록을 보내지 않는다(폐쇄망이 정체성이다). 그 원칙을 지키면서도 셀 수 있는
숫자가 있다 — **남이 세어 주는 숫자**다.

* GitHub 릴리스 자산 다운로드 수(공개 API, 우리가 아무것도 안 해도 집계된다)
* winget · Scoop 설치 수(패키지 저장소가 센다)

그래서 이번 배치는 **패키지 매니페스트**를 만든다. 유통 통로가 늘고, 덤으로 숫자가 생긴다.

## 확인한 것 (계획 전)

| 확인 | 결과 |
|---|---|
| winget 매니페스트 | **없음** |
| Scoop 매니페스트 | **없음** |
| 영어 README | 있음(126줄) · 한국어판도 있음 |
| CI 배지 | 있음 |
| 저장소 토픽 | 있음(10개) |
| 제어 평면 노출 | 도움말 ▸ AI 제어 **한 곳뿐** |

## 계획 (11 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| X1 | 배포 | **winget 매니페스트 생성**(`xtask pkg`, 순수+시험) | 통로 + 남이 세는 숫자 | 낮음 |
| X2 | 배포 | **Scoop 매니페스트 생성**(자동 갱신 필드 포함) | 위 | 낮음 |
| X3 | 배포 | 매니페스트에 **SHA-256을 코드에서** 채운다 | 손으로 적으면 어긋난다 | 낮음 |
| X4 | 배포 | 릴리스 절차서에 통로를 잇는다(코드가 출처) | 문서·코드 갈라짐 재발 방지 | 낮음 |
| T1 | 셸 | `nabi cli` **도움말을 밖으로** — `--help` 한 줄 요약 | 가장 차별화된 자산이 묻혀 있다 | 낮음 |
| T2 | 셸 | 제어 평면을 **명령 팔레트**에서 찾을 수 있게 | 위 | 낮음 |
| M1 | 메뉴/IA | 도움말 첫 화면에 **무엇이 다른지** 세 줄 | 설치한 사람도 모른다 | 낮음 |
| M2 | 메뉴/IA | 처음 실행 안내에 제어 평면 한 줄 | 위 | 낮음 |
| S1 | SSH | 세션 없을 때 **빈 화면 안내**(무엇부터 할지) | 첫 5분이 이탈을 정한다 | 낮음 |
| F1 | 파일관리자 | 원격 패널 빈 상태 안내 | 위 | 낮음 |
| E1 | 에디터 | nabiPad 빈 화면에 할 수 있는 일 | 위 | 낮음 |

## 가장 조심할 것

**빈 화면 안내가 잔소리가 되면 안 된다.** 매번 뜨는 안내는 두 번째부터 방해물이다.
세션이 하나도 없을 때만, 조용한 색으로, 한 번에 읽히는 길이로 둔다.

그리고 **매니페스트는 사실과 어긋나면 안 된다.** 해시·버전·URL을 사람이 적으면
반드시 어긋나므로([[reference-nabisori-website]]의 저장소 사고와 같은 결) 코드가
릴리스 산출물에서 직접 읽어 채운다.

---

## 결과

| # | 결과 |
|---|---|
| X1·X2·X3 | `xtask pkg` — winget·Scoop 매니페스트를 **산출물에서** 만든다. 버전은 `Cargo.toml`, 저장소는 `releasetarget::app_repo()`(코드가 출처), **해시는 파일에서 직접 계산**. 의존성을 더하지 않으려고 SHA-256을 xtask 안에 두었고, 표준 시험값 셋과 `sha256sum` 실측으로 대조했다. 시험 5. |
| X4 | 릴리스 절차서에 `xtask pkg` 를 이었다. 저장소 PR은 사람이 한다(자동화하지 않는다 — 남의 저장소다). |
| — | **README가 없는 파일을 약속하고 있었다** — `nabiTerm-standalone.zip`. 어느 릴리스에도 없다(36개 전부 `setup.exe` 하나). 한·영 양쪽에서 지우고 무인 설치 스위치로 바꿨다. |
| T1·M1 | README 첫 화면에 **제어 평면**을 올렸다(`### The part you won't find elsewhere`) — 실제 명령 예시와 함께. 한국어판도 같은 자리. |
| S1 | 빈 화면(열린 탭 0)에 **이 프로그램이 무엇인지** 두 줄. 단추 두 개뿐이라 처음 온 사람이 아무 단서 없이 멈추던 자리였다. |

### 이미 있어서 만들지 않은 것

* 빈 화면 자체(단추 둘)는 있었다 — 글만 더했다.
* 팔레트에 "도움말 ▸ AI 제어 열기"가 이미 있었다(`palettedispatch.rs`).
* 영어 README·CI 배지·저장소 토픽 10개도 이미 있었다.

이번에는 계획 전에 넷 다 확인하고 표에 적어 두었다 — 배치 W에서 두 번 헛짚은 뒤에
[[feedback-engineering-discipline]]에 적은 규율을 지킨 첫 배치다.

### 남은 일 (사람이 해야 한다)

매니페스트는 만들어 두었지만 **아직 어느 저장소에도 올리지 않았다.** `winget install`
이 된다고 말할 수 없는 상태이므로 README에도 적지 않았다. 올리려면:

* winget — `microsoft/winget-pkgs` 에 PR(`manifests/n/Nabisori/nabiTerm/<버전>/`)
* Scoop — 버킷 저장소를 하나 만들어 JSON을 두면 `checkver`·`autoupdate`가 새 판을 따라온다

---

## 이어서 — MCP 표면 (같은 회차에 붙임)

"MCP를 다음 배치 후보로"라고 적었다가 **확인해 보니 이미 있었다**(`nabi mcp`,
stdio JSON-RPC 2.0, 도구 13개). 또 추측으로 없다고 말할 뻔했다 — 그래서 만들기 전에
CLI 동사(28개)와 MCP 도구(13개)를 표로 맞대 봤다.

**진짜 빠져 있던 것은 SFTP였다.** 에이전트가 명령은 칠 수 있는데 파일은 못 옮겼다.
그런데 그것이야말로 다른 터미널에 없는 부분이다 — 붙어 있는 SSH 세션으로 원격 파일을
옮기는 에이전트.

| # | 결과 |
|---|---|
| Y1 | `nabi_sftp_list` · `nabi_sftp_get` · `nabi_sftp_put` 를 MCP에 열었다(13 → 16). 붙어 있는 세션을 그대로 쓰므로 새 인증이 없고, 권한 정책도 다른 동사와 똑같이 걸린다. |
| Y2 | **선언 ↔ 구현 대조 시험** — 선언된 도구를 전부 실제로 만들어 본다. 인자는 **각 도구의 스키마에서 생성**한다(시험에 손으로 적어 두면 도구가 늘 때마다 같이 고쳐야 하고 언젠가 잊는다). |
| Y3 | README 한·영에 **MCP 등록 한 줄**. "or MCP"라고만 적혀 있어 실제로 어떻게 붙이는지가 없었다. |

시험을 쓰다 `nabi_open_sftp`에서 한 번 걸렸는데 **내 시험이 인자를 빠뜨린 것**이었지
실제 불일치는 아니었다. 손으로 적은 인자 목록이 곧바로 어긋난 셈이라, 그 자리에서
스키마 파생으로 바꿨다([[feedback-derive-tables-from-source]]).
