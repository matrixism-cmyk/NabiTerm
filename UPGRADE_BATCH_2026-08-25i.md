# 배치 2026-08-25i — 11 phase, 다섯 분야

v0.1.468 직후. grep으로 걸러 낸 결과 이번에 남은 빈 곳들이다.

## 벤치마킹 근거

* **명령 블록 복사** — Warp의 대표 동작. 우리는 OSC 133으로 블록 경계를 **이미 알고**
  프롬프트 막대까지 그리고 있는데, 그 블록을 통째로 집어 갈 길이 없다. AI에게 붙여
  넣으려면 명령과 출력이 함께 필요한데 지금은 마우스로 긁어야 한다.
* **사용자 정의 링크 규칙** — MobaXterm·iTerm2가 갖고 있다. 로그의 `PROJ-1234`나
  `#4821`을 이슈로 바로 여는 것은 매일 쓰는 동작인데 우리는 URL과 파일 경로만 안다.
* **원격 디스크 여유** — WinSCP·FileZilla가 보여 준다. 우리는 `free_space()`를
  **구현해 놓고 한 번도 부르지 않는다.** 만들어 두고 안 쓰는 것은 없는 것과 같다.
* **세로 눈금(column ruler)** — VS Code의 `editor.rulers`. 지난 배치에 화면 접는 폭을
  냈으니 그 짝이 빈다. 접지 않고도 규약 폭을 넘는 줄이 보인다.
* **HEX 비교** — 두 이진 파일이 어디서 갈리는지. 펌웨어·인코딩 문제를 볼 때 쓴다.

## phase

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| T1 | 셸 | **명령 블록 복사** — 명령+출력을 통째로(마크다운 코드블록 포함) | 위 | 중 |
| T2 | 셸 | **사용자 정의 링크 규칙**(정규식 → URL 틀), Ctrl+클릭으로 열기 | 위 | 중(정규식) |
| T3 | 셸 | 링크 규칙 **설정 UI + 잘못된 규칙 즉시 표시** | 저장하고 나서 안 되는 것보다 낫다 | 낮음 |
| S1 | SSH | **세션 도달성 일괄 확인** — 폴더의 서버들이 지금 살아 있는가 | 접속하기 전에 안다 | 중(동시 연결) |
| S2 | SSH | 확인 결과를 세션 목록에 표시 | 결과가 목록에 없으면 다시 봐야 한다 | 낮음 |
| F1 | 파일관리자 | **원격 디스크 여유 표시**(이미 있는 `free_space` 활용) | 위 | 낮음 |
| F2 | 파일관리자 | 업로드 전 **공간 부족 경고** | 절반 올라가고 실패하는 것을 막는다 | 중 |
| M1 | 메뉴/IA | **세션 우클릭 메뉴 정리**(항목이 늘었다) | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | **세로 눈금**(80·100·120열) | 위 | 낮음 |
| E2 | 에디터 | **HEX 비교** — 두 파일이 처음 갈리는 자리 | 위 | 중 |
| E3 | 에디터 | 선택 영역 **끝 공백 정리** | 저장 시 전체 정리는 있는데 선택만은 없다 | 낮음 |
