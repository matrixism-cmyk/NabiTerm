[package]
name = "nabi-types"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
# 문자 폭 표(textcol) — 터미널 그리드(alacritty_terminal)와 같은 기준을 쓰기 위해 같은 크레이트.
unicode-width = { workspace = true }
