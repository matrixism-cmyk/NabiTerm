[package]
name = "nabi-ssh"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
nabi-i18n = { workspace = true }  # 에러 문구 3어화(trc, T8-1).
nabi-types = { workspace = true }
nabi-pty = { workspace = true }
nabi-proto = { workspace = true }
russh = { workspace = true }
tokio = { workspace = true }
crossbeam-channel = { workspace = true }
bytes = { workspace = true }
tracing = { workspace = true }  # known_hosts 를 못 고쳤을 때 왜인지 남긴다.
rand = "0.10"  # 키 생성(keygen.rs — ssh-key rand_core 0.10 정합)
