# 업그레이드 배치 L — 2026-08-26

앞 배치(k, v0.1.471) 이후. 계획 전에 grep으로 기존 구현을 확인했고, **이미 있는 것은 뺐다** —
코드 접기(`editbuffold.rs`)·스니펫·세션 로그·다단 점프(`build_jumps`가 이미 `,` 체인을 판다)·
자동 터널 개통(`autofwd.rs`)은 전부 구현돼 있었다.

## 이번에 걸린 진짜 결함

`reconnect.rs::do_reconnect`가 세션을 **이름 없이** 다시 만든다:

```rust
self.connect_saved(nabi_session::SavedSession {
    name: String::new(), folder: None, kind, on_connect: None, ...
});
```

`start_auto_forwards`는 **세션 이름을 열쇠로** 터널 규칙을 찾는다. 이름이 비었으니 아무것도
찾지 못한다 — 즉 **자동 재접속이 성공해도 그 세션의 터널은 돌아오지 않는다.** 접속 후
자동 실행 명령(`on_connect`)도 함께 사라진다.

자동 재접속은 "자리를 비운 사이 알아서 돌아와 있는 것"이 목적인데, 돌아온 자리에 터널이
없으면 DB에 붙어 있던 도구가 조용히 죽어 있다. 사용자는 재접속이 됐다고 보고 있으므로
원인을 찾기 어렵다.

## 계획 (12 phase)

| # | 분야 | 항목 | 근거 | 위험 |
|---|---|---|---|---|
| S1 | SSH | **재접속 시 터널·접속후명령 복구** — 출처(kind)로 저장 세션을 되찾는다 | 위 결함 | 중 |
| S2 | SSH | **세션별 환경변수**(ssh `SendEnv`) — 접속 시 전달 | 서버가 언어·도구를 가려 씀 | 중 |
| T1 | 셸 | **오래 걸린 명령 알림** — 임계 초과 명령이 끝나면 알린다 | `cmd_start`·`last_duration` 이미 있음(재사용) | 낮음 |
| T2 | 셸 | **스크롤백 표식** — 지금 자리에 표를 남기고 표 사이를 오간다 | 긴 로그에서 되짚을 길이 없다 | 중 |
| T3 | 셸 | 표식을 팔레트·메뉴에 일관 노출 | 드리프트 금지 | 낮음 |
| F1 | SFTP | **원격 파일 찾기** — 현재 경로 아래 재귀(상한·취소) | 목록을 손으로 파고들 수밖에 없다 | 중 |
| F2 | SFTP | **전송 끝 요약** — 몇 건 성공·실패, 실패 목록 바로 재시도 | 큐가 길면 무엇이 실패했는지 묻힌다 | 낮음 |
| M1 | 메뉴/IA | 이번 배치 기능의 메뉴·팔레트 노출 정리 + 중복 점검 | 매 배치 IA phase | 낮음 |
| E1 | 에디터 | **두 파일 비교**(diff) — 순수 코어 + 나란히 보기 | HEX 비교는 있는데 텍스트는 없다 | 중 |
| E2 | 에디터 | **열(사각) 선택** — rope 편집기 한정 | 오래 미뤄진 A3. 자체 페인터라 가능해졌다 | 높음 |
| E3 | 에디터 | 접기 UI 노출 점검 — 코어는 있는데 길이 있나 | "만든 것을 쓰기" | 낮음 |
| E4 | 에디터 | diff 결과에서 그 줄로 점프 | 비교만 되고 못 가면 반쪽 | 낮음 |

## 조심할 것

- **E2(열 선택)는 위험도가 높다.** 선택 모델이 (시작,끝) 한 쌍이라 사각 선택은 다른 모양이다.
  복사·삭제·입력 경로가 전부 그 모양을 알아야 하므로, **읽기(복사)까지만** 먼저 하고
  쓰기(사각 입력)는 다음 배치로 미룰 수 있다. 무리하면 정렬 회귀가 난다(과거 선례 있음).
- S1은 **이름이 같은 세션이 여럿일 때** 어느 것을 고를지 정해야 한다. `pane_tag`가 이미
  "출처가 같은 것 중 가장 위험한 표식"을 고르는 선례를 만들었다 — 같은 방식으로 안전한 쪽.

---

## 결과 (v0.1.472)

### 한 것

| # | 결과 |
|---|---|
| S1 | `reconnectsess.rs` — 출처(kind)로 저장 세션을 되찾아 **그대로** 쓴다. 필드를 골라 담지 않는 것이 핵심이다. 골라 담기 시작하면 세션에 새 필드가 생길 때마다 이 자리를 함께 고쳐야 하고, 잊으면 재접속에서만 조용히 빠진다(이번 결함이 그랬다). |
| S2 | `nabi-ssh/envvars.rs` + `session_env` 설정 + 세션 우클릭 편집 창. 답을 기다리지 않고 보낸다 — 서버의 `AcceptEnv` 거절은 오류가 아니다. |
| T1 | `slowcmd.rs` — 알림 자체는 있었다. **창이 뒤에 있을 때를 놓치던 것**(빌드 걸어 놓고 브라우저로 넘어가는 가장 흔한 경우)과 10초 하드코딩을 고쳤다. |
| T2·T3 | `scrollmark.rs` + `scrollmarkui.rs` — 절대 줄 번호로 표를 남기고 오간다. `nabi-vt::top_abs_line`을 새로 만들어 왕복(`scroll_to_abs_line`↔`top_abs_line`)을 시험으로 묶었다. |
| F1 | `sftpfind.rs` + `sftpfindui.rs` — **새 프로토콜을 만들지 않았다.** 동기화가 쓰는 `SftpListTree`를 그대로 쓰고 결과를 거른다. 이름만 맞춘다(폴더 이름으로 자식이 딸려 나오면 찾기가 아니다). |
| F2 | `xfersummary.rs` — 묶음이 끝나면 한 줄 요약. 파일 하나는 조용히 지나간다(그런 알림이 쌓이면 진짜 알림도 무시하게 된다). |
| M1 | 표식은 편집 메뉴의 하위 묶음으로(항목 넷을 늘어놓지 않는다), 원격 찾기는 팔레트에도. 노출 경로를 표로 훑어 빈 곳을 메웠다. |

### 곁다리로 잡은 진짜 버그 셋

계획에 없었지만 코드를 읽다 나왔다. 셋 다 "세션에 저장한 설정이 접속까지 살아오지 못한다"는 같은 결이다.

1. **재접속하면 터널이 사라진다**(S1의 계기) — 이름 없는 세션으로 다시 지어 붙였다.
2. **에이전트 포워딩이 저장 세션에서 적용되지 않았다** — `tabops.rs`가 `fwd`를 출처 기록에만 넣고 `params`에는 싣지 않았다. 켠 사람은 켜졌다고 믿고 원격에서 git을 쓴다.
3. **점프 체인이 경로마다 달랐다** — 터미널은 `parse_connect`로 한 홉만, SFTP는 `build_jumps`로 전부. 같은 세션을 어떻게 여느냐에 따라 결과가 달랐다.

### 계획에서 뺀 것 (이미 있었다)

grep으로 확인해 **만들지 않았다.** 만들었다면 같은 일을 두 곳이 하게 됐을 것이다.

- **E1·E4 텍스트 diff** — `difflines.rs`·`diffopen.rs`·`diffopenui.rs`가 이미 있고 팔레트·보기 메뉴·분리 창에서 전부 닿는다.
- **E3 코드 접기** — `editbuffold.rs` 코어와 우클릭 메뉴가 있다.
- **다단 점프** — `build_jumps`가 콤마 체인을 판다(다만 위 3번대로 한쪽만 쓰고 있었다).
- 세션 로그·스니펫·자동 터널·재귀 권한 변경도 이미 있었다.

### 미룬 것

**E2 열(사각) 선택.** 선택 모델이 (시작,끝) 한 쌍이라 사각 선택은 다른 모양이고, 복사·삭제·입력 경로가 전부 그 모양을 알아야 한다. 이번 배치에 욱여넣으면 정렬 회귀가 날 위험이 커서 다음 배치로 넘긴다 — **읽기(복사)까지만 먼저** 하는 쪽으로.

### 검증에서 걸린 것

설정을 화면에만 넣고 검색 표에 안 넣어 `the_index_matches_the_screens`가 실패했다.
v0.1.466에 만든 드리프트 게이트가 제 몫을 했다.
