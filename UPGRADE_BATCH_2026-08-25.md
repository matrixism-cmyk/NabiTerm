# 업그레이드 배치 계획 — 2026-08-25 (v0.1.462~)

목표: **상용 제품 이상.** 경쟁 제품 조사 + 코드베이스 전수 확인 후, 이미 있는 것을 빼고
진짜 빈 곳만 골랐다(후보의 절반 가까이가 이미 구현돼 있었다 — 임포터 5종·SFTP 동기화·
전송 큐 개별 일시정지·재접속·세션 그룹 이동·단축키 도움말).

## 이번 배치에서 **하지 않는 것**(이미 있음 — 확인 완료)

| 후보 | 실제 위치 |
|---|---|
| PuTTY·MobaXterm·FileZilla·ssh config·Xshell 세션 가져오기 | `putty.rs` `mobaxterm.rs` `filezilla.rs` `sshconfig.rs` `xshell.rs` |
| SFTP 디렉터리 동기화·비교 | `syncplan.rs` `sftpsyncui.rs` `editsftp.rs` |
| 전송 큐 개별 일시정지·순서변경·재시도 | `sftpqueue.rs` |
| 전송 이력(큐가 비워진 뒤에도) | `sftphistory.rs` |
| 자동 재접속 · SSH keepalive | `events.rs` · `nabi-ssh` |
| **SSH 에이전트 인증**(Pageant + OpenSSH 파이프) | `nabi-ssh/src/agent.rs` |
| 세션 그룹 만들기·이름변경·**드래그 이동** | `nabi-session/src/groups.rs` · `sidebar.rs` |
| 점프 호스트·멀티홉 | `nabi-ssh-ext/src/proxy_jump.rs` · `sshjump.rs` |
| 세션 그룹(폴더) + 그룹 이동 | `sessionctx.rs` `sessionsmenu.rs` |
| 명령 팔레트 · 스니펫 · 브로드캐스트 · 워크스페이스 스냅샷 | `palette.rs` `toolsmenu.rs` `broadcastview.rs` `worksnap.rs` |
| 첫 실행 온보딩 · 단축키 도움말 | `onboarding` · `helppages::shortcut_page` |

## 이번 배치 (14 phase, 5분야)

### ① 셸/터미널
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| A1 | **세션별 색·태그 영속** — 저장 세션에 색/태그 필드를 두고 사이드바·탭·상태바에 반영 | 감사 결과 탭 색은 **실행 중에만·pane별**로 있고(`app.rs tab_colors`) 저장되지 않는다 — 껐다 켜면 사라지고, 연결 전에는 보이지도 않는다. 운영 서버에 실수로 명령 치는 것을 막는 **안전장치** | 중 / 낮음 |
| A2 | **위험 세션 확인** — 색이 '운영'인 세션에 붙여넣기·브로드캐스트 시 한 번 확인 | A1이 장식으로 끝나지 않게. 사고는 붙여넣기에서 난다 | 하 / 낮음 |
| A3 | **모든 pane 통합 검색 + 검색어 이력** | pane 하나 안 검색은 있으나(`find.rs`) 전체 검색과 검색어 이력이 없다. "그 오류 어느 창에서 봤더라"가 실제 작업 흐름 | 중 / 중간(성능) |

### ② SSH
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| B1 | **에이전트 포워딩**(`auth-agent-req@openssh.com`) | 감사 결과 로컬 에이전트 인증(Pageant·OpenSSH 파이프)은 **이미 있다**(`nabi-ssh/src/agent.rs`). 포워딩만 빠져 인증 스택의 마지막 조각이다. 원격에서 다시 git/ssh를 쓰는 흐름에 필수 | 상 / 중간 |
| B2 | **설정·전체 백업/복원** | 지금은 **세션만** 내보낼 수 있다(`nabi-session/src/export.rs`). 설정·테마·스니펫·known_hosts가 빠져 PC를 옮기면 다시 꾸며야 한다 | 중 / 낮음 |

### ③ 파일관리자·SFTP
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| C1 | **WinSCP 사이트 가져오기** — ini + 레지스트리 양쪽 | 한국에서 가장 널리 쓰이는 SFTP 클라이언트인데 유일하게 빠진 임포터 | 중 / 낮음 |
| C2 | **가져오기 한 화면** — 설치된 프로그램을 자동 탐지해 목록으로(지금은 메뉴에 흩어짐) | 전환 장벽 제거가 임포터의 목적인데 찾기 어려우면 의미가 없다 | 중 / 낮음 |

### ④ 메뉴·IA (매 배치 1+ phase 필수)
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| D1 | **설정 검색** — 설정 창 상단 검색으로 항목·탭 바로 찾기 | 설정 항목이 계속 는다. VS Code·Chrome·Windows 설정 전부 검색이 기본 | 중 / 낮음 |
| D2 | **접근성 1단계** — egui AccessKit 켜기 + 주요 화면 키보드 전용 탐색 | 감사 결과 **완전 공백**(accesskit·a11y·고대비 0건). 공공/기업 조달에서 자주 요구되고, 상용 제품의 최소선이다 | 상 / 중간 |
| D3 | **앱 안 진단 로그 보기** | 지금은 탐색기만 열어 준다(`helppages.rs`). 원격 지원 때 "그 폴더 열어서 파일 찾아 보내 주세요"는 상용 제품의 응대가 아니다 | 중 / 낮음 |
| D4 | **줄 수 경고 정리** — 소프트 250 초과 13건을 모듈 분리로 해소 | 우리 규율이 경고를 쌓아 두면 규율이 아니다 | 하 / 낮음 |

### ⑤ 에디터(nabiPad)
| # | 항목 | 근거 | 난이도/위험 |
|---|---|---|---|
| E1 | **용량 무제한 편집기 화면 연결** — textview·textkeys·textbar + 열기 경로 | 엔진(63시험)은 끝났는데 **사용자가 아직 쓸 수 없다.** 이번 배치 최우선 | 상 / 중간 |
| E2 | **미저장 새 문서 크래시 복구** — 경로 없는 문서도 주기 저장 후 복구 제안 | 지금 autosave는 **경로 있는 파일만**. 새 문서는 강제 종료 시 통째로 사라진다 | 중 / 낮음 |
| E3 | **인코딩으로 못 적는 글자 안내** — 교차 검토 #10의 UI 쪽 마무리 | 엔진은 거절하는데 사용자에게 이유를 안 알려 주면 "안 눌린다"가 된다 | 하 / 낮음 |

## 순서

E1 → A1·A2 → E2·E3 → C1·C2 → D1·D3 → B2 → A3 → D2(접근성) → B1(에이전트 포워딩) → D4 → 게이트 → 배포

큰 것 둘(D2 접근성 · B1 포워딩)은 뒤에 둔다 — 앞의 것들이 먼저 사용자 손에 닿는다.

## 검증

매 phase 순수 함수 + 단위 시험. B1은 실제 에이전트 대상 `#[ignore]` 통합 시험.
배포 전: clippy 0 · lines 0 · unsafe-audit · 전체 테스트 · e2e · verify-release.
새 엔진·프로토콜(B1 에이전트 프로토콜)은 배포 전 **codex 교차 검토** 1회.

---

## 진행 상황 (갱신)

| Phase | 상태 | 비고 |
|---|---|---|
| E1 용량 무제한 편집기 화면 연결 | ✅ | 97MB/180만 줄이 실제 앱에서 0.39초. e2e 게이트에 영구 등록. 임계값 512MB→**64MB** |
| A1 세션별 색·태그 영속 | ✅ | `SavedSession.tag` + 사이드바 띠 + 상태바 + 우클릭 |
| A2 운영 세션 붙여넣기 확인 | ✅ | 붙여넣기 단일 관문(`paste_needs_confirm`) |
| E2 미저장 문서 크래시 복구 | ✅ | `padrecover` — 설정과 무관하게 늘 남긴다 |
| E3 못 적는 글자 안내 | ✅ | E1에 포함 |
| C1 WinSCP 가져오기 | ✅ | 레지스트리·ini 양쪽. 한글 이름 깨짐 결함을 시험이 잡았다 |
| C2 가져오기 한 화면 | ⏳ | |
| D1 설정 검색 | ⏳ | |
| D3 앱 안 진단 로그 보기 | ⏳ | |
| B2 설정·전체 백업/복원 | ⏳ | |
| A3 모든 pane 통합 검색 | ⏳ | |
| D2 접근성 1단계 | ⏳ | |
| B1 에이전트 포워딩 | ⏳ | |
| D4 줄 수 경고 정리 | ⏳ | |
